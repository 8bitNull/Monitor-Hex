# v2.4.2 验收

- 卡片、表格、详情页已无收藏组件；源码中的收藏状态、样式、翻译已移除。旧浏览器收藏键不再读取。
- 首页卡片备注按 ; / ；分割、去空白，以稳定颜色标签显示；与详情页使用同一标签组件。
- 无备注无费用时不生成底部区域；手机换行、长词换行、无水平溢出。备注按纯文本显示，不执行 HTML。
- 详情导航删除空出的收藏列；表格节点名称利用原星标空间。
- 构建、静态检查、数据与翻译检查通过。
- Chrome 原有 54 项通过；新增备注用例修正异步页面加载等待后单独通过。Edge 完整 55 项通过（47.2 秒）。
- 浅色/深色卡片截图更新并检查；320/390/1440 宽度备注、空备注、长备注、分隔符、颜色一致性与旧收藏偏好兼容已验证。
- 仅本地模拟接口验收，未部署生产站点。
