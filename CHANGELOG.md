# Monitor HEX 当前版本

## 0.0.16

- 精修详情顶部：地区、系统和在线时长归入辅助行；Agent 版本移至硬件资料，长备注支持展开与收起。
- 手机与中等宽度详情工具栏采用两行，延迟平滑及线路批量操作独立分组。
- 延迟线路收进选择面板，显示选中数量与摘要，选中项优先；支持 Escape 关闭及焦点恢复。
- 新增设备资料自动／展开／折叠设置：自动在 900px 以下折叠，桌面展开；手动选择持久保存，支持导入导出和重置。
- 资料按硬件与系统、网络与流量、费用与到期展示；复制成功及失败提示不再挤动资料行。
- 统一加载、空数据和失败状态的绘图区高度，减淡网格；手机可点选数据提示，多线路长提示可滚动。
- Chrome 97/97、Edge 相关路径 33/33 通过；README 预览全部更新为当前版本。

## 0.0.15

- 新增完整／精简卡片显示预设，依据当前六项开关显示预设名称，其他组合显示自定义；手机独立方案可单独应用。
- 设置按常用外观、卡片信息、指标与图标、首页模块、线路、提醒、高级外观和偏好管理组织；预设与信息开关优先展示。
- 明确本浏览器保存、手机跟随关系与恢复操作范围。旧配置继续保留，无新增持久化字段。
- 微调首页间距、国旗与标题对齐、备注和价格标签节奏；保留桌面同排等高和手机自然高度。
- 补充选中勾号、统一键盘焦点、手机备注入口点击区域和减少动态效果支持。
- 新增预设、持久化、导入导出、手机独立、短屏横屏、键盘及默认配色对比度检查。完整记录见 REVIEW-v0.0.15.md。

## 0.0.14

- 桌面同排卡片等高，辅助信息区自然靠下；手机保留内容自然高度。
- 网速数字与单位分开占位，极窄屏统一分行，避免单位变化导致高度跳动。
- 初始加载使用结构化占位；离线、过期、等待上报、请求失败、探测超时与真实零值明确区分，减少重复提示。
- 同节点、同图表、同时间范围刷新失败保留上次成功图表，重试成功后清除提示；请求期间禁用重复刷新，旧响应不会覆盖新范围。
- 返回首页按节点恢复滚动和焦点；支持浏览器后退、页面返回、尺寸变化与节点重排，节点消失时回退滚动位置。
- 不增加偏好字段，现有自定义设置继续保留，实时排序规则不变。
- Chrome 71/71、Edge 相关路径 27/27，八种宽度、1/6/30/100 节点通过；截图与验收记录见 REVIEW-v0.0.14.md。

## 0.0.13

- 新增六项首页卡片辅助信息开关，关闭后移除对应空行与分隔线。
- 新增手机跟随／独立设置，保留独立方案，支持 720px 边界即时切换。
- 新增桌面自动／2／3／4 列，按可用宽度自动降列，手机保持单列。
- 背景和质感选项收进默认折叠的高级外观。
- 新配置支持站点默认、浏览器偏好、导入导出及两种恢复操作；升级默认布局不变。
- Chrome 58/58、Edge 关键路径 18/18 通过；64 种辅助信息组合均验收。

## 0.0.12

- 移除桌面地图右下角的信息图标。
- 手机卡片采用左侧备注、右侧价格的布局，适配长备注与大金额。
- 线路标题默认只显示名称和箭头，原生选择列表继续明确全局继承选项。
- 减少延迟区域留白，保留 44px 操作区域；标准手机卡片降低 16px。
- 连接数与在线时长合并试排未采用，保留两行以保障窄屏可读性。
- Chrome 46 项回归与 1 项新增用例通过；Edge 17 项相关用例通过。

## 0.0.11

- 精修详情页标题、状态、资源数值、网速单位与辅助资料层级。
- 统一历史图表线宽、网格、提示框和选中状态，提高主要控件触控尺寸。
- 设置按外观、显示内容、提醒重新分组，优化滚动关闭及 320px 英文布局。
- 修复英文详情页“网速”标签漏译。
- 增加六尺寸、双语言、深浅色与四种指标样式检查；详见 REVIEW-v0.0.11.md。

## 0.0.10

- 精修首页总览与节点卡片的字号、留白、数值对齐和信息层级。
- 默认资源样式调整为两列细进度条；卡片按身份、资源、网速、延迟、辅助资料排列。
- 优化 320px 窄屏的资源标签、数值和上下行网速单位对齐，中英文、深浅色和四种指标样式均完成检查。
- 保留已有明确保存的指标样式，修复旧偏好迁移和选择与默认值相同的样式时丢失选择的问题。
- 统一延迟及丢包标注，保持高负载入口的 44px 触控区域。
- 更新预览图、手机前后对比图与验收记录。Chrome 全量 40/40、Edge 关键路径 14/14 通过。

正式版本编号为 0.0.x。来源与许可见 NOTICE.md，验证记录见 ACCEPTANCE.md 与 REVIEW-v0.0.10.md。
