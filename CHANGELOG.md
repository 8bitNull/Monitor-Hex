# Monitor HEX 当前版本

## 未发布

- 移除公开页的集中“显示与偏好”入口；明暗、语言、主要探测线路和表格列改为就地调整。
- 旧版浏览器配置仅保留仍可在前台调整的个人偏好，站点级设置统一跟随后台主题配置。

## 0.2.6

- 站点显示设置改由后台“主题”页统一管理，并持久化到 hub；主题公开页读取这些配置作为默认值。
- 公开页的“显示与偏好”只保存访客个人选择，个人偏好可覆盖对应的站点默认值。
- 兼容未提供主题设置接口的旧版 hub，继续使用安装包内的默认配置。

## 0.2.5

- 首页卡片备注显示前三项紧凑标签，其余以“+N”表示，点击仍可查看完整备注。
- 桌面表格在有横向溢出时提供左右查看按钮，便于找到隐藏的列。
- 手机总览的节点筛选和离线筛选扩大点击区域，减少误触。
- 表格列设置改为桌面／手机切换，便于分别配置两套列。

## 0.2.4

- 首页卡片备注改为紧凑标签，显示前两项和剩余数量；完整备注仍可在弹窗中查看。
- 详情页上行、下行、TCP 和 UDP 的标签与数值向内收拢，提升指标阅读密度。
- 桌面端延迟图例与统计说明图标同排，采样范围和采样中位值集中展示。

## 0.2.3

- 首页地图改为紧凑高度，可展开查看，选择会保存在当前浏览器。
- 离线节点卡片和详情页突出状态与最近上报时间，隐藏失效的实时资源读数；线路探测按采样时间独立标记新旧。
- 手机表格在当前结果都没有备注时隐藏空备注列；表格列与布局设置在手机端更容易找到。
- 主题配置导入导出覆盖外观、显示、网络、表格列、语言、线路偏好和地图高度；保留旧版外观配置导入，全部重置前增加确认。

## 0.1.27

- 首页 TCP、UDP 名称与数值组成紧凑居中指标组，固定8px间距，中间增加14px浅色短竖线，桌面与手机同步。

## 0.1.26

- 首页备注统一单行灰色文字，超出省略；桌面悬停显示全文，点击打开完整备注弹窗，支持关闭和 Escape 返回焦点。
- 用量、到期时间、在线时长三栏标题、数值与辅助信息居中对齐。

## 0.1.25

- 精简首页三栏：用量与额度分行，移除首页额度条及百分比，统一标题与主值层级，改用浅色短分隔线；长备注两行折叠，超出可展开，费用顶部对齐。

- 首页卡片调整为“周期用量｜到期时间｜在线时长”三等分，细竖线分隔；备注与费用同排，左备注、右费用，桌面与手机同步。

- 精简详情页延迟线路操作：移除顶部线路菜单与独立统计线路下拉框，图例统一控制曲线显隐。
- 摘要直接标明所属线路，下方丢包线路选择同步控制摘要；单线路自动跟随，隐藏当前线路自动切换，全部隐藏时清除统计显示。
- 手机摘要的线路名称与数值分行，保留线路展开和空态恢复入口。

## 0.1.24

- 落实14项体验审查意见：手机三列表格与双行筛选工具栏；在线/离线快捷筛选；可收起总览；搜索结果直达。
- 资源与延迟共用双行手机工具栏；桌面完整线路图例，手机前四条可展开；统计线路与丢包轨道双向同步。
- 缩放后就近解释不可统计的丢包；时间手柄提供读屏起止时间；失败、空历史、未选线路提供恢复操作。
- 统一卡片密度与备注排版；设置更名“显示与偏好”，高级背景折叠，标明桌面专属选项；增加地图帮助与本地高负载记录说明。
- 保留旧字段/列配置兼容，新增总览收起偏好，补充中英文与回归检查。

## 0.1.23

- 首页用量与到期按标题、主数值、状态三层对齐，增加线性图标、3px 额度条和真实百分比；近额度与超额使用不同提示色。
- 无限额度不显示进度条，有有效重置日时显示下一次重置日期；兼容短月份与跨年，窄屏隐藏百分比。
- 首页单线路将丢包移至标题右侧，线路数并入紧邻选择器的入口，趋势填满可用宽度。
- 详情延迟曲线及图例统一实线，固定颜色区分线路；精简重复文字，操作及统计说明集中展示。
- 延迟页前台每 30 秒同步，后台暂停、返回立即更新；保持线路、平滑与按时间戳保存的缩放区间，失败保留旧图，避免并发重复请求。
- README、实际截图及验收说明同步更新。

## 0.1.22

- 顶部工具栏按效果图采用“资源／延迟、分隔线、线路选择、平滑显示”，精简图标并统一间距。
- 桌面排版修订：补齐效果图中的左侧线路／右侧统计同排，收紧纵向留白；修复缩略图坐标轴影响主图时间刻度，更新截图并重新发布同版本安装包。

- 首页手机延迟柱条按剩余宽度展示，PC 与手机端指标和数值垂直居中。
- 详情页线路图例改为勾选开关，新增采样均值、P95、丢包摘要与统计口径说明。
- 时间选择器加入真实趋势缩略图、44px 手机手柄与恢复完整范围操作，摘要及丢包轨道同步缩放。
- 手机延迟工具栏改为双行，线路开关双列排列；P95 收入说明，保留可关闭的数据提示。
- 统计基于原始采样桶中位值；缩放范围缺少包数量时不推算丢包率，平滑不改变摘要。
- 同步 README、实际截图、版本元数据与验收说明；最终构建、lint、业务与翻译检查、Chrome 全量 192/192 通过。

## 0.1.21

- 首页上下行由分段活动条改为最近 60 秒的真实采样趋势线，缩小数值并集中显示 TCP／UDP、周期用量和到期信息。
- 在线时间和价格同排、价格右对齐；备注独占底部并左对齐换行，已开启的信息无需再展开“更多资料”。
- 详情页顶部按资源、网络、用量与账期分组，历史图表全宽展示；平板和手机自适应排列。
- 详情页加入负载资源项；在线时间、费用和备注移入概览，下方设备资料保留硬件与网络信息，避免重复。
- 更新中英文字段、响应式及数据状态回归测试、README 和实际页面截图。
- 生产构建、lint、业务单元测试和翻译检查通过；Chrome 全量 191 项中 182 项首轮通过，9 项旧布局断言更新后复测全部通过。

## 0.1.20

- 主题设置的卡片信息区域删除冗余说明，资料密度标签与下拉框保持同一行，减少纵向空白。
- 设置中的选择框统一覆盖鼠标和键盘焦点状态，使用中性边框替代黑色焦点框；外观选项继续保留主题色选中提示。
- PC 首页地区地图移除“收起地图”控件，地图开启后直接完整展示，缩放、拖动、全屏和地区筛选保持不变。
- README、验收说明、版本元数据和安装包同步更新；静态检查、业务测试、507 处字面量翻译检查、生产构建与 Chrome 全量端到端回归 184/184 通过。

## 0.1.19

- 流量用量统一使用后端按节点 `traffic_reset_day` 计算的当前周期 `month_used`；旧接口没有该字段时兼容 `month_rx/month_tx`。
- 首页卡片、详情页和表格流量排序统一使用当前周期流量逻辑，非 1 号重置周期显示周期起始日期。
- 新增跨月、短月份及 15/31 号重置日测试，README、版本元数据和安装包同步更新。
- 静态检查、业务测试、508 处字面量翻译检查、生产构建与 Chrome 全量端到端回归 184/184 通过。

## 0.1.18

- 首页单线路不再显示采样时间跨度说明；延迟指标与当前数值收紧排布并减少线路区域空白。
- 首页延迟默认显示最近 1 小时，网络设置可切换最近 1 小时、6 小时或 24 小时，旧偏好继续兼容并支持导入导出。
- 首页延迟行按线路名实际宽度排列，消除线路名与指标示意之间的额外空白；新增间距回归断言。
- 延迟指标柱条常规宽度提升至 88–140px，窄屏保留紧凑尺寸，改善指标拥挤问题。
- 主题设置中的配色、卡片预设、指标样式、卡片密度和延迟预设统一使用中性边框；选中状态改用淡主题色背景与文字提示，键盘焦点保留高对比轮廓。
- 生产构建、lint、业务测试、509 处字面量翻译检查与 Chrome 全量端到端回归 183/183 通过。

## 0.1.17

- 设置收敛为“外观、显示内容、网络、偏好”四类；卡片布局、指标样式和背景质感集中到外观，首页模块与详情页集中到显示内容，语言和偏好管理集中到偏好。
- 延迟设置改为“原有分档 80/160ms”“跨境参考 150/300ms”和“自定义”三种入口；保留原有偏好字段和导入兼容性。
- 偏好导入／导出与恢复操作按备份、恢复分组；高级外观继续直接展示并移除重复标题与分隔线。
- 首页卡片延迟信息改为线路名、采样指标示意、延迟时长同一行排列，多线路和窄屏继续自适应。
- 多线路首页不再重复显示采样时间跨度说明，并收紧线路之间的垂直间距。
- 生产构建、lint、业务测试、505 处字面量翻译检查与 Chrome 全量端到端回归 182/182 通过。

## 0.1.16

- 高级外观设置直接展示；未选中的外观选项使用中性边框，选中状态才使用主题色强调。
- 首页选择两条或三条线路时，每条当前显示线路均展示完整的延迟指标示意。
- 表格排序控件移入顶部视图工具栏，置于“卡片／表格”切换左侧；移动端保持紧凑布局并提供图标化的恢复默认操作。
- README、验收说明和版本元数据同步更新；Chrome 全量端到端回归 181/181 通过。

## 0.1.5

- 新增桌面八列分组表格，合并节点状态和双向实时网速，保留旧用户独立列及逐方向隐藏设置。
- 网络质量展示共享阈值的延迟等级与后端 24h 丢包；上下行继续使用真实 Mbps，未知、离线、超时及旧记录分别呈现。
- 支持上传、下载、延迟、丢包独立排序和恢复默认顺序；主动质量排序复用有界并发读取，普通浏览保持可见节点懒加载。
- 增加手机概览、网络、费用预设及完整节点资料查看；桌面固定表头和名称列，详情返回恢复表格内部位置。
- 标准行高 68px，统一数值对齐、流量额度条与到期信息层级；表格样式集中管理，减少重复行渲染。
- 列配置升为 v4，外观配置仍为 v3；迁移保留旧列选择并尝试保存会话内回退快照。

## 0.1.4

- 新增概览资料密度：费用、到期与流量常驻，连接数、运行时长及备注按需展开；旧版偏好保留完整资料，可手动应用推荐显示。
- 收紧首页资源、实时网速、延迟柱条与手机总览，统一数字层级；真实 Mbps、零值、缺失、超时及丢包语义不变。
- 修复“查看全部线路”只打开默认线路的问题，支持全部线路深链接、刷新和异步数据；详情增加线路名、虚实线及末次读数常驻图例，多线路菜单可搜索。
- 手机详情提供显式返回；设备资料取消二级折叠，费用状态位于续费下方；历史加载采用稳定高度骨架。
- 设置分为外观、卡片、网络、其他；提供明确的延迟视觉预设并保留未应用草稿。偏好版本升级为 3，兼容旧版导入及显式选择。
- 手机搜索增加关闭入口、结果计数和焦点恢复；手机表格独立保存列选择；地图默认关闭且可收起；地区展示本地化名称。
- Chrome 全量 169 项、Edge 关键 17 项通过；最后图例微调后 Chrome 定向 16 项通过。桌面标准卡片约 662→549px，手机总览约 262→175px，详见 ACCEPTANCE.md。
- 本次仅更新主题；不包含 monitor-probe 后台改造，也不引入主动测速或虚构的三网实际吞吐数据。

## 0.1.3

- 首页上下行固定为蓝／绿 12 段等高活动条，使用共同比例；资源样式设置不再切换网络指标的表达方式。
- 实际上下行速率统一为 Mbps，涵盖首页、总览、表格、详情趋势、历史坐标与提示；低正值不显示为零。
- 首页主线路延迟固定为按时间戳排列的采样柱条，所有卡片共用 200／500 ms 刻度，支持全局黄／红阈值；超时、缺失和超刻度分别表达。
- 设置支持新字段验证、旧配置默认补齐、导入导出与恢复行为；详情页继续保留实时网速曲线和丢包轨道。
- PC 端硬件与系统、网络与流量、费用与到期标题和内容增加 16px 间隔。
- Chrome 全量 163 项通过，新增网络样式固定、偏好持久化、低速与异常状态、阈值及封顶读数回归。

## 0.1.2

- 首页网速新增方向文字，突出数值并保持单位位置；默认细条仍表示相对峰值，兼容已有外观选择。
- 首页主线路采用最多 40 个真实样本的轻量延迟曲线，标注采样跨度；丢包改为同排读数与异常提示。
- 详情页新增最近 60 秒上下行趋势，按上报时间去重、限制缓存并保留断线缺口。
- 延迟历史新增可选择线路的丢包时间轨道，跟随缩放，支持点按、悬停和键盘逐样本阅读；零丢包与未知明确区分。
- 线路菜单扩大名称空间并对齐读数；费用到期状态提示移到续费下方。
- 修复加载卡片交换高度时返回首页的定位偏移，保留用户操作后停止自动定位的行为。

## 0.1.1

- 详情页历史工具栏统一资源／延迟标签、资源指标、平滑显示、线路菜单、时间范围与刷新；手机采用紧凑单行布局。
- 线路菜单简化为逐项多选，移除搜索、批量操作和仅看／恢复入口。
- 修复 601–1199px 资源指标文字隐藏后按钮缺少可访问名称的问题，并补充悬停提示。
- 修复线路多选时列表重排和滚动跳动；菜单打开期间保持顺序，重新打开时已选优先。
- 补充中英文资源按钮和线路顺序、滚动、焦点及图表提示回归测试，同步使用文档。

## 0.1.0

- 首页桌面端搜索位于主题设置左侧，手机端收束为放大镜入口并在屏幕中央打开搜索框；长列表支持筛选清除和返回顶部操作。
- 手机节点卡片优先展示核心指标，辅助信息收进按需展开区；详情资料按硬件与系统、网络与流量、费用与到期分组，CPU 与 IP 等值按屏幕对齐。
- PC 端详情页移除重复的设备资料标题和 CPU 复制按钮；IPv4、IPv6 等适合复制的资料继续保留复制入口。
- 手机资源／延迟工具栏将 `1h`、`6h`、`24h`、`7d` 作为完整时间组排列；极窄屏只移动整组，不拆分 `7d`，刷新按钮始终保持独立且不遮挡。
- 费用到期状态补充正常、即将到期、已过期和未设到期的视觉反馈；补充搜索、折叠、分组和响应式几何回归验收。

## 0.0.22

- 桌面详情页将设备资料收束为一个统一外框，内部按硬件与系统、网络与流量、费用与到期组成三列网格，并用细分隔线保持信息关系。
- 实时状态改为三项主指标、双向网速和负载/TCP/UDP辅助信息的分层网格，减少横向跳跃和无效留白。
- 历史图表工具栏在桌面端铺满模块顶部，延迟与资源标签使用轻量下划线，平滑与线路选择继续浮在曲线右上角。
- 手机保留资料折叠、复制、线路选择和图表提示交互；未新增偏好字段。

## 0.0.21

- 详情页改为设备资料、实时状态、历史图表的上下阅读顺序，桌面资料使用硬件、网络、费用三列网格，手机保持单列阅读。
- 设备资料与实时状态拆分，移除重复的负载、进程和容量展示；模块标题补充图标，保留动态负载与连接数据在实时区域。
- 线路批量操作收进线路选择面板，统一浅色卡片、边框和操作区域，效果图中的深色身份与线路模块改为白底。
- 新增详情网格、白底模块、重复信息和移动端无溢出的回归验收；README 和验收记录更新为 v0.0.21。

## 0.0.20

- 线路支持“仅看此线路”和恢复原选择，保留首页继承、空选择与多选语义。
- 提示框以小色点识别系列，名称和数值使用中性色，丢包辅助信息弱化；延迟精度与未知值保持。
- 同范围刷新失败时，手机和桌面均直接显示上次成功时间；新范围不会显示其他范围的时间。
- 手机长 CPU、IPv6 等资料独占标签下方整行，完整显示并可复制。
- Escape 关闭资源/延迟提示，关闭按钮焦点返回图表；搜索与节点切换焦点行为回归通过。
- README 仅引用 v0.0.20 预览，验收入口统一见 ACCEPTANCE.md。

## 0.0.19

- 详情实时概况突出 CPU、内存和硬盘，负载与连接数移入辅助分组，统一数值和进度条对齐。
- 长名称展开按钮移至标题下方，展开备注改用轻量自然换行文本，费用资料降低背景权重。
- CPU、IPv6 等可复制长值与按钮分列，避免文字占用按钮空间，保持完整复制和稳定反馈。
- 延迟提示按线路面板顺序排列，不再使用名称字典排序；桌面曲线高亮继续保留。
- 1200px 起展示低存在感的更新时间，失败明确保留旧记录；切换范围清除其他范围的成功时间。
- 侧栏收窄试排未采用，保留宽桌面 320px 栏宽与现有绘图区高度。
- README 更新为当前版本，验收入口统一见 ACCEPTANCE.md。

## 0.0.18

- 桌面绘图区调至 360px，缩减顶部及内部留白，让标准内容的资料首行进入 1440×900 首屏。
- 宽屏设备资料按硬件、网络、费用约 1.2∶1.1∶0.8 分配宽度；中等屏幕费用改为紧凑横栏，各卡片自然高度。
- 图表提示将时间和关闭按钮合并为标题行，名称与数值分列，延迟最多一位小数；提示根据绘图区宽度收缩，避免窄屏裁切。
- 线路颜色结合有限虚实线组合，图例与曲线保持一致，范围切换和显隐不改变线路样式。
- 手机减少区块与辅助信息间距，保留字体和 44px 操作区域；加载、空记录与失败画布统一图标、对齐和文案位置。
- 无新增偏好字段；安装包与截图按仓库发布流程生成，验收入口统一见 ACCEPTANCE.md。

## 0.0.17

- 桌面设备资料移至完整底栏，按宽度显示两列或三列，手机继续保留自动折叠偏好。
- 手机资源和延迟图表增加 44px 关闭按钮，支持点外部关闭、范围切换清除提示和多线路内部滚动。
- 超过 6 条线路显示名称搜索；搜索不改变选择，批量操作覆盖全部线路，摘要展示两个名称和剩余数量。
- 线路在面板打开时按已选优先排列，多选期间保持顺序与滚动位置，支持 Escape 和焦点恢复。
- 超长名称最多两行并提供独立展开入口；复制控件统一对齐，成功反馈 2 秒后消失，处理重复点击与卸载后的迟到结果。
- 可选图表摘要经 320px 网络图试排后暂不加入，优先保留清晰的绘图区。
- README 只展示当前版本截图；验收入口统一见 ACCEPTANCE.md。

## 0.0.16

- 精修详情顶部：地区、系统和在线时长归入辅助行；Agent 版本移至硬件资料，长备注支持展开与收起。
- 手机与中等宽度详情工具栏采用两行，延迟平滑及线路批量操作独立分组。
- 延迟线路收进选择面板，显示选中数量与摘要，选中项优先；支持 Escape 关闭及焦点恢复。
- 新增设备资料自动／展开／折叠设置：自动在 900px 以下折叠，桌面展开；手动选择持久保存，支持导入导出和重置。
- 资料按硬件与系统、网络与流量、费用与到期展示；复制成功及失败提示不再挤动资料行。
- 统一加载、空数据和失败状态的绘图区高度，减淡网格；手机可点选数据提示，多线路长提示可滚动。
- Chrome 97/97、Edge 相关路径 33/33 通过；README 预览全部更新为当前版本。

## 0.0.15

- 新增完整／精简卡片显示预设，依据当前六项开关显示预设名称，其他组合显示自定义；手机独立方案可单独应用。
- 设置按常用外观、卡片信息、指标与图标、首页模块、线路、提醒、高级外观和偏好管理组织；预设与信息开关优先展示。
- 明确本浏览器保存、手机跟随关系与恢复操作范围。旧配置继续保留，无新增持久化字段。
- 微调首页间距、国旗与标题对齐、备注和价格标签节奏；保留桌面同排等高和手机自然高度。
- 补充选中勾号、统一键盘焦点、手机备注入口点击区域和减少动态效果支持。
- 新增预设、持久化、导入导出、手机独立、短屏横屏、键盘及默认配色对比度检查。

## 0.0.14

- 桌面同排卡片等高，辅助信息区自然靠下；手机保留内容自然高度。
- 网速数字与单位分开占位，极窄屏统一分行，避免单位变化导致高度跳动。
- 初始加载使用结构化占位；离线、过期、等待上报、请求失败、探测超时与真实零值明确区分，减少重复提示。
- 同节点、同图表、同时间范围刷新失败保留上次成功图表，重试成功后清除提示；请求期间禁用重复刷新，旧响应不会覆盖新范围。
- 返回首页按节点恢复滚动和焦点；支持浏览器后退、页面返回、尺寸变化与节点重排，节点消失时回退滚动位置。
- 不增加偏好字段，现有自定义设置继续保留，实时排序规则不变。
- Chrome 71/71、Edge 相关路径 27/27，八种宽度、1/6/30/100 节点通过；截图与验收入口统一见 ACCEPTANCE.md。

## 0.0.13

- 新增六项首页卡片辅助信息开关，关闭后移除对应空行与分隔线。
- 新增手机跟随／独立设置，保留独立方案，支持 720px 边界即时切换。
- 新增桌面自动／2／3／4 列，按可用宽度自动降列，手机保持单列。
- 背景和质感选项收进默认折叠的高级外观。
- 新配置支持站点默认、浏览器偏好、导入导出及两种恢复操作；升级默认布局不变。
- Chrome 58/58、Edge 关键路径 18/18 通过；64 种辅助信息组合均验收。

## 0.0.12

- 移除桌面地图右下角的信息图标。
- 手机卡片采用左侧备注、右侧价格的布局，适配长备注与大金额。
- 线路标题默认只显示名称和箭头，原生选择列表继续明确全局继承选项。
- 减少延迟区域留白，保留 44px 操作区域；标准手机卡片降低 16px。
- 连接数与在线时长合并试排未采用，保留两行以保障窄屏可读性。
- Chrome 46 项回归与 1 项新增用例通过；Edge 17 项相关用例通过。

## 0.0.11

- 精修详情页标题、状态、资源数值、网速单位与辅助资料层级。
- 统一历史图表线宽、网格、提示框和选中状态，提高主要控件触控尺寸。
- 设置按外观、显示内容、提醒重新分组，优化滚动关闭及 320px 英文布局。
- 修复英文详情页“网速”标签漏译。
- 增加六尺寸、双语言、深浅色与四种指标样式检查。

## 0.0.10

- 精修首页总览与节点卡片的字号、留白、数值对齐和信息层级。
- 默认资源样式调整为两列细进度条；卡片按身份、资源、网速、延迟、辅助资料排列。
- 优化 320px 窄屏的资源标签、数值和上下行网速单位对齐，中英文、深浅色和四种指标样式均完成检查。
- 保留已有明确保存的指标样式，修复旧偏好迁移和选择与默认值相同的样式时丢失选择的问题。
- 统一延迟及丢包标注，保持高负载入口的 44px 触控区域。
- 更新预览图、手机前后对比图与验收记录。Chrome 全量 40/40、Edge 关键路径 14/14 通过。

正式版本编号为 0.0.x。来源与许可见 NOTICE.md，验证记录见 ACCEPTANCE.md。
