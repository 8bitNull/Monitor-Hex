# Monitor Next v2 设计约束

唯一正式视觉：浅灰底、白色表面、细边框。深色仅替换语义颜色，不更换布局。

## 样式职责

- src/styles/tokens.css：颜色、字体、圆角、间距、控件高度与明暗语义。
- src/styles/components.css：基础交互、总览、工具栏、卡片、线路和密度规则。
- src/styles/supporting.css：表格、地图、设置、详情、背景与图表辅助规则。
- src/index.css：样式入口和 Tailwind 映射，不累积版本覆盖补丁。

## v2.2 详情页

详情按实时指标、历史图表、完整资料排序，styles/detail.css 负责页面专用布局。延迟图框桌面 360px、手机 280px，图例独立排布，不从滚动位置推导高度。默认选中首页实际线路；比较操作不写回首页偏好。图例丢包必须注明当前查询窗口，不能一律称为 24h。

## v2.1 卡片与首页

资源固定 2×2 排列，负载数值保持原始 1 分钟值，进度条按核心数归一。上下行对齐，单位弱化；流量、单线路、时长与到期依次分区。首页不再设置排序/分组/收藏过滤/批量刷新操作行；全局线路位于设置。

## 组件与数值

Select 保留原生键盘/手机选择体验；ResourceMetric 集中资源状态；NodeIdentity 的 Status/Country 供卡片、表格和详情共用。
复用现有 Button、Card 与语义化原生控件，不添加第二套组件库。原生与共享控件统一使用全局尺寸、焦点、边框及交互规则。

页面标题 22px，卡片标题 16px，正文 14px，辅助 12px，仅容量/标签等次级信息 11px。
面板圆角 12px，控件 8px，标签 6px。默认卡片内边距 16px，紧凑 12px。
桌面主要控件 36px，手机 44px；信息图标支持点击，图表 hover 不改变卡片高度。
品牌色只影响强调元素；危险、警告、上下行颜色分别使用独立语义变量。

## 数据状态

加载、无记录、请求失败、上次数据、较旧采样、超时、未知丢包分别显示，不互相替代。
首页只展示单节点选择的线路；详情保留所有已返回的目录项，包括无采样线路。
窗口丢包禁止平均采样桶百分比。失效显式选择不静默回退。

## 修改纪律

修改所属组件规则，删除不再使用的旧声明，不在文件末尾追加版本修补层。
新增控件必须有可访问名称；长字段在详情页完整可读。
调整设计后先检查截图差异，再更新基线；禁止以更新基线替代错误排查。
历史布局标识只为导入兼容，不得重新引入 CSS 分支。四种资源图形仅改变表达，不能改变状态含义。


## v2.4 体验规则

首页优先展示节点；390×844 默认首屏必须包含第一张卡片主要资源。概览共用一个容器，筛选在手机上两行。表格常规行高约 60px，延迟最多两行。地图不再重复完整卡片。设置使用原生模态对话框以支持键盘焦点限制和 Escape；移动端全屏。资源/状态/延迟的短标签不可因图标化丢失关键含义。历史区域预留稳定高度防止懒加载位移；详情只固定图表本体，不留多余外围占位。新增规则集中于 styles/experience.css。
