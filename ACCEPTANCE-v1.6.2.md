# v1.6.2 首页去重验收

合并顶部操作行，移除重复计数、在线色条、重复负载值、仪表合计和未设到期占位。单一系统无筛选条件时隐藏无效筛选；筛选后显示结果范围。异常状态、地区入口、详情信息和延迟刷新保留。

构建、代码检查、七组单元及 311 个翻译调用检查通过。Chrome/Edge 完整回归各 33/33 通过；最后调整状态按钮文案和负载辅助值后，两浏览器相关 7/7 增量回归通过。已检查桌面和手机尺寸截图。安装包版本、路径、大小检查通过。

验收采用本地模拟 API；未上传线上服务器，手机尺寸为浏览器模拟。
