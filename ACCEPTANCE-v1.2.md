# Monitor Next v1.2.0 第一阶段验收报告

日期：2026-09-19。结论：**开发完成，本地功能验收通过；真实 hub 部署联调未执行，不计为通过。**

## 交付与结果

| 项目 | 已实现及验收结果 |
|---|---|
| 表格视图 | 卡片/表格/地图切换、固定名称列、列显隐、表格独立横向滚动；通过 |
| 排序 | 名称、状态、CPU、内存、硬盘、上下行、所选线路延迟、到期时间；升降序、默认顺序恢复、缺失值置后；通过 |
| 延迟增强 | 主要探测选择、其他线路展开、历史曲线、采样时间、超时/旧记录提示；通过 |
| 状态保留 | 搜索、筛选、排序、视图、列设置、主要探测在当前标签页保存，详情返回与刷新后恢复；通过 |
| 连接状态 | 首次连接、实时、轮询、断开及最后更新时间；静默连接超时、畸形推送回退与恢复；通过 |
| 中文地区搜索 | 如“日本”匹配 JP 节点；通过 |
| 多节点 | 100 节点筛选和全部节点延迟摘要读取；请求并发最多两个；通过 |

本版本仅变更主题，无 hub 数据库和管理接口改动。

## 自动检查

- TypeScript 与生产构建：通过。
- 代码检查：通过，无警告。
- 四组单元检查：格式化、实时指标容错、延迟统计/缓存/并发、排序/缺失值/地区搜索，全部通过。
- Chrome：10/10 场景通过，0 失败、0 跳过、0 不稳定用例。
- Edge：10/10 场景通过；最终曲线文案调整后追加相关场景 2/2 通过。
- 桌面、390px 手机表格截图已人工查看；320px 和 390px 自动验证无整页横向溢出，滚动限制在表格容器内。

浏览器场景包括：

1. 搜索、筛选、外观持久化、地图、详情深链及刷新。
2. WebSocket 推送及畸形/缺失指标隔离。
3. 明暗模式、手机宽度和空列表。
4. 延迟汇总、超时、多线路、空记录和错误。
5. 站点失败重试、关闭公开页后的跳转。
6. 表格/卡片数值一致、列显隐、排序、恢复默认、详情返回与刷新后的状态保留。
7. 主要探测选择、延迟排序、超时置后、历史图悬停和点击。
8. 实时 → 轮询 → 请求失败 → 轮询恢复 → 实时恢复，切换视图不增加连接。
9. 100 节点筛选、历史摘要全量加载以及并发计数。
10. 静默 WebSocket 超时和畸形消息回退。

## 性能记录

环境为本机 Windows，已安装 Chrome/Edge，生产构建配合模拟 API。

| 指标 | 结果 |
|---|---|
| 100 节点筛选 | 最后一轮 Chrome 31.6ms，Edge 28.9ms，含自动化操作开销 |
| 历史请求并发 | 最大 2；延迟响应模拟下实际计数验证 |
| 历史缓存 | 约 60 秒，卡片/表格/排序共享 |
| 主 JS gzip | 85.95 kB；相对 v1.1.0 的 82.14 kB 增长约 4.6% |
| CSS gzip | 8.96 kB |

这些时间是单次本地观测，不是各设备上的性能承诺。地图与详情独立加载；启用延迟排序会加载所有节点摘要，显示进度，排序可能随数据到达调整。普通浏览优先加载可见节点。

## 数据口径

- 延迟为最新历史采样桶的值，不是点击时重新 ping。
- 24h 丢包率来自后端窗口汇总，不平均色条的桶百分比。
- 波动为相邻有效采样桶平均绝对差，不等于原始探测抖动。
- 色条是最近采样序列，不代表等间隔时间；曲线按采样时间绘制，超时标红叉，超过两小时的间隔断开。
- 超过两小时的采样、读取失败和未知延迟不参与有效延迟排序；旧记录保留时间提示。
- 默认“各节点首条线路”可能不是同一目标；横向比较应选择同一主要探测线路，缺少该线路时不自动换另一条。
- 浏览偏好保存在当前标签页的 sessionStorage。存储不可用时页面仍运行，但刷新后不保证恢复。

## 安装与回滚

后台「主题」上传 `monitor-next-v1.2.0.tar.gz` 后选择 Monitor Next。标准命名 `theme.tar.gz` 内容与版本包一致。源码 ZIP 不能作为主题上传。

压缩包根目录为 theme.json、dist/、预览与许可文件。回滚可切换默认主题，或上传已保留的 v1.1.0 包，不涉及数据库迁移。

实际部署后补查公开页、详情刷新、真实探测数据、WebSocket 反向代理、主题升级与回滚。

## 未验证事项

- 没有用户实际 hub 地址或测试实例，未执行真实后台安装及探针、反向代理联调。
- 手机检查为浏览器视口模拟，非 iOS/Android 真机；Safari、Firefox 未测。
- 已测试 100 节点，未将 500 节点压力测试或长期稳定性计为通过。

本报告的“通过”仅指明确列出的本地检查。
