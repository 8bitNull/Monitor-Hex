# Monitor Next v1.5.2：界面收敛

- 标题、连接状态与更新时间合并，移除装饰性英文标题和宣传副标题。
- 搜索、视图、状态和地区筛选放入同一浏览面板；收藏、系统、分组、排序、主要线路、表格列选项收进“筛选与排序”。
- 折叠栏显示启用的高级条件，开合状态在当前标签页内保存；浏览器禁用存储仍能使用。
- 延迟采样时间、读取时间与颜色说明放入可点击的“采样信息”；旧记录及读取失败提醒仍直接显示，历史曲线独立可用。
- 经典卡片缩小圆环和间距，移除悬停上浮。自定义背景下不再为每排工具单独绘制底板。

## 验收

构建、代码检查、七组单元检查及 284 个翻译调用覆盖通过。Chrome 原 24 项通过中的 23 项及新增收敛场景通过；公开页关闭跳转场景因旧测试等待 load 与重定向竞争失败，改为等待 commit 后检查目标 URL，单项复测通过，累计覆盖 25 项。Edge 完整 25/25 通过。

新增检查覆盖默认折叠、启用条件提示、采样信息展开、中英界面在 320/390/768/1440px 的收起/展开宽度。检查桌面、深色、移动预览；延迟自动刷新、后台恢复、地图与详情回归保留。

基于本地模拟 API，未操作用户线上服务器，手机尺寸为浏览器模拟。安装新版主题包后即可使用；源码 ZIP 不用于后台上传。
