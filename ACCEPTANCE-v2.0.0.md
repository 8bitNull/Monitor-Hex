# v2.0.0 验收记录

日期：2026-09-20；Windows，本地构建 + 模拟公开 API，Chrome / Edge。

## 结果

- TypeScript/Vite 构建通过，CSS 优化无警告；oxlint 通过。
- 8 组单元/数据检查通过，294 处字面翻译调用覆盖检查通过。
- Chrome：41/41 通过（20.8 秒）；最终整理版本 Edge：41/41 通过（44.7 秒）。
- 四张固定节点视觉基线：1440/390 宽、浅色/深色，Chrome 建立后比较通过，Edge 比较通过。
- 两种密度 × 四种指标 × 明暗 × 320/1440，以及中英 320/390/768/1440 无页面横向溢出。
- 五项审计问题专项通过：95% CPU 数字/所有图形同色；新用户采用站点 graph/palette；长 CPU 在 390px 完整换行；元数据无采样线路可选且详情列出；未知 loss 保持破折号。
- 保留刷新/自动更新、两请求并发限制、历史断线、节点独立线路记忆、错误恢复、禁用存储、背景玻璃降级、地图/表格/收藏/配置导入导出。

## 测量

摘要缓存基准，每节点 1440 样本（Node.js 合成数据，非完整浏览器/生产性能）：

| 节点数 | 首次摘要 | 10 次缓存读取 |
|---|---:|---:|
| 20 | 2.37 ms | 0.07 ms |
| 100 | 12.97 ms | 0.07 ms |
| 500 | 25.83 ms | 0.22 ms |

100 节点筛选操作含自动化耗时：Chrome 32.6ms，Edge 37.6ms。该结果不能外推为生产网络或 500 节点全页渲染性能承诺。
缓存测试验证无关节点订阅不收到通知、已淘汰快照不可读。可见/已挂载订阅或请求中的节点被保留，其他快照上限 200。
最终 CSS 产物约 56.59 KB（gzip 11.08 KB），之前约 86.70 KB，减少约 35%。

## 兼容与边界

只有一套正式视觉；历史皮肤/布局参数兼容导入，保留在内部迁移信息中，但不再切换视觉结构，导出和站点示例移除退役项。旧紧凑布局缺少密度时映射紧凑。收藏与节点线路使用原存储键。
公开接口没有返回的后台线路无法凭空发现；本次合并的是实际返回的 probes 和 ping ID。
本轮不包含生产站点上传/切换；真实 hub 安装与实际监控数据还需部署后确认。此前 v1.9.0 安装包保留可回退。
