# v1.7.0 验收记录

日期：2026-09-20。环境：本地示例 API、Chrome / Edge；未连接或部署到用户实际 monitor 实例。

## 结果

- TypeScript / Vite 生产构建、oxlint 均通过。
- 7 组基础测试和 307 处翻译调用覆盖检查通过。
- Chrome 原有 33 项回归通过；新增首页与高负载验收 2 项通过。资源标签微调后重跑 9 项布局/首页用例，线路标题校正后重跑 8 项线路/首页用例。
- Edge 完整 35 项通过。最后的线路自动选项标题校正由 Chrome 对应线路回归覆盖。
- 320 / 390 / 768 / 1440 宽度、五种卡片布局、四种指标样式、明暗主题和中英文兼容检查通过。
- 默认总览三张卡片；手机并排。390×844 首屏能看到第一个节点核心资源、网速与延迟色带。
- 地区/系统筛选收进展开区，已选条件外部可清除；展开地区标签不横向溢出。
- 更多信息默认收起，详细布局默认展开；累计流量、处理器等信息仍能访问。
- CPU 85% 出现高负载提醒，下降到 84% 后消失。
- 延迟每分钟刷新、手动刷新、恢复页面可见后更新、并发上限 2、超时/旧记录提示、历史曲线点击与键盘操作回归通过。

## 交付

主题版本 1.7.0，安装包根目录含 theme.json、dist、许可证、NOTICE 与 preview.png。源码包不含依赖、构建目录或测试生成物。安装包校验值另附。预览使用示例数据。
