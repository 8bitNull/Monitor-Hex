# v0.0.11 详情页与设置面板验收

状态：实现与客观验收完成，用户已确认继续发布 v0.0.11。

## 实施结果

- 详情页保留桌面概况栏与分析区、手机单列结构。桌面概况栏由 300px 调整为 320px，中等屏幕由 260px 调整为 280px；状态靠近节点名称，长名称仍可换行。
- 提高系统信息、设备资料与容量说明的字号；资源数值采用统一对齐，上传下载单位同时置于数字下方。辅助资料使用更柔和的底色。
- 资源与延迟图表统一线宽、网格节奏、提示框和选中控件；减少延迟范围填充的视觉强度，保留多线路颜色映射。
- 图表主要按钮与设置主要操作采用至少 44px 触控高度；时间范围、资源切换和刷新在窄屏分行排列。
- 设置按外观（含背景）、显示内容（首页模块与线路）、提醒组织；高负载单独归组，偏好管理位于底部。语言保留在顶部。
- 设置关闭按钮保持可见，滚动后可直接关闭；320px 色板为三列两行、指标样式为两列，避免不规则换行。
- 补齐英文详情页动态“网速”标签的翻译，并加入浏览器断言。
- 数据计算、离线与中断处理、偏好存储格式、导入导出和恢复默认逻辑沿用已有实现。

## 验收矩阵

| 项目 | 结果 |
| --- | --- |
| 静态检查、业务测试、生产构建 | 通过 |
| 字面量翻译检查 | 375 处通过；动态资源按钮另做英文断言 |
| Chrome 全量回归 | 46/46 通过 |
| Edge 详情、设置、语言、导航关键路径 | 22/22 通过 |
| 最终标题布局 | Chrome、Edge 各 9/9 通过 |
| 最终英文标签复核 | Chrome、Edge 各 6/6 通过 |
| 尺寸 | 320、390、430、768、1024、1440px |
| 语言与配色 | 中文、English × 浅色、深色 |
| 四种资源样式 | 六尺寸 × 两语言 × 两配色 × 四样式，共 96 组合 |
| 页面与资源数值 | 无横向溢出，实时卡片数值不越界 |
| 首屏 | 390×844 标准样本可见名称、状态、CPU 与内存 |
| 设置 | 分类跳转、唯一选项、提醒开关、滚动关闭、焦点恢复、偏好保存与重置通过 |
| 既有功能 | 指标/时间范围/线路切换、刷新失败、节点选择、地区筛选、表格与告警回归通过 |
| 数据边界 | 零值、缺失样本、采样中断、离线、长名称/备注、无历史、失败重试等既有用例通过 |

全量回归后仅继续调整标题状态布局与一项英文文案；分别重跑相关用例，不将不同轮次测试累计成总用例数。

## 视觉证据

前后均使用相同固定演示数据与时间（2026-09-20 12:00 北京时间）。基线来自 GitHub 已发布 v0.0.10 安装包，新版来自本地生产构建。

六尺寸 × 两语言 × 两配色 × 详情/延迟/设置，共 72 组截图对照。完整交互浏览页保存在本地 tests/artifacts/detail-polish/review.html；原始截图不提交仓库。

- [手机详情前后对比](screenshots/v0.0.11/mobile-detail-comparison.png)
- [手机设置前后对比](screenshots/v0.0.11/mobile-settings-comparison.png)
- [桌面详情前后对比](screenshots/v0.0.11/desktop-detail-comparison.png)
- [深色图表前后对比](screenshots/v0.0.11/dark-chart-comparison.png)

精选对比为相同缩放的上部裁切；完整新版图片保存在同目录。阅图重点为文字舒适度、数字与单位对齐、深色图表、设置分组和窄屏英文换行。主观美观度由用户确认。

## 安装包与边界

theme.tar.gz 与 theme.tar.gz.sha256 在仓库根目录生成；安装包版本为 0.0.11，短名 hex，在线更新地址保持 https://github.com/8bitNull/Monitor-Hex。

本次随正式版本同步源码、预览与验收记录。手机检查采用浏览器视口模拟，尚无实体手机、iOS Safari 或线上后端部署验收；未进行完整 WCAG 审计。
