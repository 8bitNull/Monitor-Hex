# v0.0.21 详情页模块化网格验收

2026-09-21。基线 v0.0.20（d98cf5c）。用户授权发布。

## 实施结果

- 详情页统一为“设备资料 → 实时状态 → 历史图表”的上下阅读顺序；桌面资料区使用硬件与系统、网络与流量、费用与到期三列网格，手机按单列排列并保留资料折叠偏好。
- 设备资料保留静态容量、系统、架构、网络、费用等信息，移除与实时概况重复的负载和进程值；实时模块保留 CPU、内存、硬盘、网速、负载和 TCP/UDP。
- 资料与实时模块增加 Lucide 图标和统一白色卡片边界；效果图中的身份模块和线路概览由深色改为白底，使用边框、色彩和间距区分层级。
- 线路首页、全部显示、全部隐藏操作收进线路选择面板，保持 44px 操作尺寸和原有选择语义。
- 新增 `detail-grid.spec.ts`，覆盖模块顺序、三列网格、白色背景、标题图标、重复信息移除和移动端溢出。

## 验收结果

| 项目 | 结果 |
| --- | --- |
| lint、业务测试、翻译覆盖、生产构建 | 通过；438 处字面量翻译调用检查通过 |
| Chrome 全量 | 142/142 通过 |
| 详情页专项 | 59/59 通过，覆盖 320–1920px、深浅色、线路操作、提示、资料和图表 |
| Edge 全量 | 141/142 通过；唯一失败为并发下既有节点排序返回定位用例 |
| Edge 稳定性复测 | 串行 13/13 通过，包含失败用例；其余 Edge 全量用例通过 |
| 视觉验收 | 桌面资料三列、实时状态全宽、历史图表全宽；手机资料折叠后顺序正确，白色模块无横向溢出 |
| 安装包 | `theme.tar.gz` 与 `theme.tar.gz.sha256` 已生成，根目录包含 `theme.json`、`dist`、许可证 |

## 视觉证据

- `screenshots/detail-light.png`
- `screenshots/latency-dark.png`
- `screenshots/v0.0.21/mobile-detail-light.png`
- `screenshots/v0.0.21/desktop-detail-dark.png`
- `tests/artifacts/v0.0.21-detail-grid-concept.png`（白色模块效果图）

测试使用本地演示 API、模拟故障和 Chrome/Edge 桌面浏览器手机视口。实体手机、iOS Safari、系统软键盘、动态地址栏与真实服务器部署未验证。

## 发布边界

版本 0.0.21，短名 `hex`，在线更新地址 https://github.com/8bitNull/Monitor-Hex。附件 `theme.tar.gz` 与 `theme.tar.gz.sha256`。
