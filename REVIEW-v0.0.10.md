# v0.0.10 首页精修验收记录

状态：实现与客观验收完成，用户已确认继续发布 v0.0.10。

## 已实现

- 总览统一主数值、辅助文字和图标层级；高负载入口保留 44px 触控区域，去除突兀底框。
- 节点卡片采用身份 → 资源 → 网速 → 延迟 → 辅助资料的顺序，减少重复分隔线和加粗。
- 新安装且未指定指标样式时使用 4px 连续进度条。明确保存的用户选择及站点配置继续优先。
- 主要数值和单位统一对齐；320px 下资源标签与数值分行，上下行单位同时换行，保持两侧数值基线一致。
- 默认数值 15px、网速 20–22px、单位 12px、辅助信息 11–12px；可选圆环数字 14px，极简数字 22px。
- 修复旧迁移强制覆盖指标样式，以及显式选择恰好等于站点默认值时丢失选择的问题；恢复默认仍重新跟随站点。
- 不改变资源、流量、网速、丢包和到期的计算方式。

## 客观检查

| 项目 | 结果与证据 |
| --- | --- |
| 静态检查、业务单元测试 | 通过 |
| 翻译覆盖 | 371 处通过 |
| Chrome 全量浏览器测试 | 40/40 通过 |
| 最终字号与网速基线复核 | Chrome 5/5 通过，覆盖 80 种尺寸／语言／主题／样式组合 |
| Edge 关键路径 | 14/14 通过；最终样式额外复核 5/5 |
| 尺寸 | 320×844、390×844、430×932、768×1024、1440×1000 |
| 样式 | 进度条、圆环、分段柱条、极简数字；中英文和深浅色 |
| 布局 | 无页面横向溢出；资源数值不越界；上传下载数值基线一致 |
| 第一屏 | 390×844 可见首张节点卡片名称、状态及 CPU／内存指标 |
| 设置兼容 | 新默认、旧／新存储格式、站点配置变化、等于默认的显式选择、恢复默认均通过 |
| 数据状态 | 正常、高负载、零值、离线、过期、缺失、大数值、长名称／备注、到期、无延迟、失败和超时分别检查 |
| 现有功能 | 地区、视图、详情、线路快捷入口、告警历史、语言、明暗与焦点恢复回归通过 |
| 构建与安装包 | 生产构建及打包通过；theme.json 为 0.0.10，更新地址保留 |

## 视觉证据

固定演示数据及时间：2026-09-20 12:00（北京时间）。旧版对照来自已发布的 v0.0.9 安装包；新版来自当前构建。截图包括五种尺寸 × 两种语言 × 深浅色，共 20 组前后对照，另含完整节点卡片。

- [手机浅色前后对比](screenshots/compare-mobile-light.png)
- [手机深色前后对比](screenshots/compare-mobile-dark.png)
- 全部对比浏览页保留在本地 tests/artifacts/refinement/review.html（不提交仓库）。
- 原始截图位于 tests/artifacts/refinement/baseline 与 candidate。
- 数据异常截图位于 tests/artifacts/refinement/state-*.png。

这些截图是演示数据，不代表服务器真实状态。已提供浅色与深色前后对比供用户审阅。

## 验收边界与发布门槛

- 手机验证采用桌面浏览器视口模拟，尚未在实体手机及 iOS Safari 上验收。
- 未进行线上部署或实际后端联调。没有进行完整 WCAG 审计。
- 历史存储中没有 graph 字段时，无法还原当时是否曾主动点击与默认值相同的样式；按既有规则视为跟随站点。已有明确字段，以及本版之后的显式选择均保留。
- 用户审阅后已确认继续发布；本次同步正式预览与验收记录。
