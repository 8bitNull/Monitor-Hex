# v0.0.16 详情页阅读与图表体验验收

2026-09-21。用户授权实施、验收后直接发布。基线为 v0.0.15（58c562d）生产构建。

## 实施结果

- 顶部以名称、切换节点和在线状态为主，地区、系统与运行时长置于辅助行。Agent 版本进入硬件资料。超过三条或单条超过 48 字的备注提供展开入口，收起时使用短标签，展开显示完整内容。
- 宽桌面采用一行图表工具栏；手机及 900–1199px 中等桌面采用固定两行，避免左侧概况栏占位后拥挤。资源/延迟在第一行，时间范围与刷新在第二行。延迟专属的平滑、全部显示/隐藏、首页线路操作位于独立区域。
- 线路选择使用可展开面板，常态显示已选数量和最多三个名称摘要，选择面板内优先排列已选线路，长列表可滚动。展开不改变绘图区高度；Escape 收起并回到入口。图表仍使用既有稳定线路颜色。
- 绘图区统一使用视口对应高度；加载、空记录、首次失败共用与正常数据相同的内容画布。刷新失败继续保留同范围历史记录，错误提示在画布之外展示。网格进一步弱化，曲线不增加入场动画。900px 以下使用点选提示，宽桌面悬浮提示；长提示支持内部滚动。
- 新配置字段 detailInfoMode 为 auto/expanded/collapsed，默认 auto。900px 以下默认折叠，900px 起展开；手动切换保存为明确选择。恢复外观保留此偏好，全部重置恢复站点默认。导入导出及旧配置回退已接入。
- 设备资料展示为硬件与系统、网络与流量、费用与到期。仅显示后端返回的公开 IPv4/IPv6 值，不把协议标记当作地址。CPU 与地址支持复制，成功勾号及失败说明在控件附近显示，不新增资料行。

## 验收结果

| 项目 | 结果 |
| --- | --- |
| 静态检查、业务测试、生产构建 | 通过；418 处字面量翻译检查通过 |
| Chrome 全量 | 97/97 通过，含新增详情阅读 16 项 |
| Edge 相关回归 | 33/33 通过：详情阅读、返回定位/刷新稳定性、节点导航 |
| 布局矩阵 | 320、390、430、720、899、900、1024、1440、1920px；每种宽度检查中英文、深浅色和四种资源样式 |
| 手机首屏 | 390×844 标准身份信息下，四种资源样式的历史工具栏均完整进入首屏 |
| 工具栏 | 1200px 以下两行；宽桌面一行；按钮至少 44×44px，双语无溢出 |
| 资料偏好 | 899/900 边界、手动选择刷新保存、导出导入、恢复外观保留、全部重置通过；业务测试验证非法值回退和旧配置默认 |
| 多线路 | 1、3、20 条线路选择、全选、全部隐藏、已选摘要、Escape 与焦点恢复通过；选择不改变图高 |
| 画布稳定性 | 手机资源加载、空记录、首次失败与正常数据的内容高度差不超过 2px；提示区允许额外占高 |
| 长内容 | 80 字名称、8 条长备注的展开/收起、窄屏无横向溢出通过；既有超长硬件资料与计费数据回归通过 |
| 复制 | 成功与拒绝写入都验证反馈；资料行高度不变；成功时剪贴板内容由既有导航测试核对 |
| 手机数据提示 | 模拟手机视口点选资源图显示数值、单位，提示框不越出屏幕 |
| 数据正确性 | 既有零值、缺失样本、超时断线、范围独立、取消与迟到响应、刷新失败保留曲线、重复刷新阻止通过 |
| 首页兼容 | 显示预设、手机独立方案、64 种信息组合、地图/表格/地区、返回原节点、1/6/30/100 节点布局全量通过 |

矩阵是分层覆盖，不是所有尺寸、线路数、数据状态的完整笛卡尔积。工具栏在中等桌面使用双行是可用宽度约束下的明确取舍。既有测试中 Agent 所在位置和线路入口交互已按新界面更新，数据保持与选择语义的断言保留。

## 视觉证据

- [手机详情](screenshots/v0.0.16/mobile-detail-light.png)
- [手机深色详情](screenshots/v0.0.16/mobile-detail-dark.png)
- [手机延迟](screenshots/v0.0.16/mobile-latency-dark.png)
- [线路选择](screenshots/v0.0.16/routes-mobile-light.png)
- [资料展开设置](screenshots/v0.0.16/detail-settings-light.png)
- [失败保留历史](screenshots/v0.0.16/history-retained.png)
- [手机前后对比](screenshots/v0.0.16/compare-mobile-detail.png)
- [桌面前后对比](screenshots/v0.0.16/compare-desktop-detail.png)

固定数据对比由 scripts/capture-detail-reading.mjs 生成，完整原始截图与旧构建存放本地 tests/artifacts/detail-reading，不进入安装包。README 的 12 张预览全部来自当前构建，不包含旧版本图或前后对比图。

## 发布与边界

版本 0.0.16，短名 hex，更新地址 https://github.com/8bitNull/Monitor-Hex。附件 theme.tar.gz 与 theme.tar.gz.sha256。

验证使用演示 API、模拟故障和桌面 Chrome/Edge 的手机视口。实体手机、iOS Safari、系统键盘、动态地址栏、真实安全区及线上后端部署未验证，不计为通过。自定义外部背景与任意透明度组合没有逐一做视觉认证。
