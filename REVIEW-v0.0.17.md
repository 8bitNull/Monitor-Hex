# v0.0.17 详情布局与交互验收

2026-09-21。基线 v0.0.16（b0fc04c）。按用户授权实施、验收并直接发布。

## 落地结果

1. 900px 起资料区移至桌面完整底栏；900–1199px 两列，1200px 起三列。硬件、网络、费用的 DOM 顺序与视觉顺序一致。手机顺序和 detailInfoMode 偏好继续保留。
2. 手机资源与延迟提示增加 44×44px 关闭按钮，点击图表外部也会关闭，切换范围/指标/节点不会沿用旧提示。多线路内容内部滚动，未增加阻止页面滚动的触摸处理。修复窄屏点击曲线时被悬浮高亮更新打断的首次取点问题；桌面继续保留悬浮高亮。
3. 线路超过 6 条时显示搜索，支持中英文名称、不区分英文大小写。搜索仅过滤列表，批量按钮明确作用于全部线路；摘要为前两个名称加 +N。打开时按已选优先排序，面板打开期间顺序固定，避免选择底部线路后滚动跳动。重新打开清空查询并重新排序。Escape 返回 summary 焦点，外部点击只关闭面板。
4. 复制按钮固定在资料行右侧，完整值可复制。成功反馈 2 秒后消失，失败显示说明并可再次点击；请求序号防止迟到结果覆盖新反馈，卸载和值变化时清理计时器并作废请求。
5. 超长名称默认限制两行，实际溢出才出现独立展开按钮；按钮与节点切换分离，完整名称作为切换按钮的可访问描述。切换节点通过既有 key 重置展开状态。
6. 可选摘要暂不加入：320px 网络图试排中，最新值、样本均值、样本峰值连同上下行与单位需要三行；固定画布内挤占刻度，增加高度又破坏现有阅读节奏。本次只作视觉原型，不引入统计逻辑或新增设置。后续如增加独立分析模式，再做统计正确性验收。

## 验收

| 项目 | 结果 |
| --- | --- |
| lint、业务测试、生产构建、打包 | 通过；425 处字面量翻译检查通过 |
| Chrome | 110/110；含本轮 13 项用例 |
| Edge | 46/46；详情布局、详情阅读、稳定性、导航 |
| 宽度矩阵 | 320、390、430、720、899、900、1024、1440、1920px；中英文、深浅色、四种资源样式分层覆盖 |
| 桌面资料 | 900、1024、1440、1920px 检查整行宽度、列数、排列顺序及无横向溢出 |
| 手机提示 | 320/390px 两种图表左中右点选，关闭、再打开、点外部、范围切换、20 条提示内部滚动；按钮至少 44px，图高不变 |
| 线路 | 既有 1/3/20 条及新增 6/7/20 条；中英文搜索、无结果、全选/全隐藏、清空搜索、Escape、焦点、摘要；底部线路多选滚动偏差不超过 1px |
| 长名称与复制 | 长名称展开/收起独立于节点切换，短名称无按钮；IPv6 剪贴板完整，2 秒反馈消失，重复点击，成功/失败行高不变 |
| 既有行为 | 偏好持久化、导入导出、899/900px 边界、返回节点定位、失败保留曲线、迟到响应、缺失样本与零值、首页卡片/地图回归通过 |

测试是分层覆盖，不是所有组合的笛卡尔积。浏览器回归使用本地演示数据和模拟故障。复制 2 秒时效使用 Playwright 可控时钟验证，避免并行浏览器后台计时节流造成假失败。

## 视觉证据

- [桌面详情](screenshots/detail-light.png)
- [手机浅色详情](screenshots/v0.0.17/mobile-detail-light.png)
- [手机深色详情](screenshots/v0.0.17/mobile-detail-dark.png)
- [线路搜索](screenshots/v0.0.17/routes-search.png)
- [手机多线路提示](screenshots/v0.0.17/mobile-tooltip.png)
- [桌面改前改后](screenshots/v0.0.17/compare-desktop-detail.png)
- [手机改前改后](screenshots/v0.0.17/compare-mobile-detail.png)
- [摘要试排（未采用）](screenshots/v0.0.17/summary-trial.png)

scripts/capture-detail-balance.mjs 从固定演示数据生成基线与当前构建截图。README 的 12 张预览均为 v0.0.17，旧版和对比图只用于历史验收，不放入 README。

## 发布边界

短名 hex；更新地址 https://github.com/8bitNull/Monitor-Hex。安装附件为 theme.tar.gz 与 theme.tar.gz.sha256。

未验证实体手机、iOS Safari、系统软键盘、动态地址栏和真实线上部署；桌面浏览器手机视口测试不替代这些验证。本次未修改后端接口或新增用户偏好字段。
