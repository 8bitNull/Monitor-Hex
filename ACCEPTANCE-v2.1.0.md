# v2.1.0 验收记录

2026-09-20，Windows / 本地模拟 API。

- TypeScript/Vite 构建、oxlint、8 组单元/数据检查通过；274 处字面翻译调用覆盖通过。
- Chrome 与 Edge 全量回归各 45/45 通过。Edge 最终运行 43.8 秒。
- 重新检查并更新四张卡片视觉基线（1440/390、浅色/深色），随后两浏览器基线比较通过。
- 新增四项验收：删除首页第二排及旧状态不隐藏节点；设置线路持久化/导入/节点覆盖优先级；2×2 资源布局与负载归一、上下行对齐、离线未知值；旧全局线路一次性迁移。
- 320/390/768/1440、中英文、明暗、两种密度、四种资源图形覆盖；自动刷新、单节点刷新、并发上限、错误恢复、禁用存储、地图、表格和长字段回归通过。
- 表格列标题排序仍可用；首页排序组件、分组、升降箭头、仅看收藏、重置排序和批量刷新已删除。卡片始终按后台默认顺序显示。
- 全局线路现在位于右上角外观设置 > 线路；收藏与节点独立线路选择保留。设置打开时读取线路目录，最多两个历史请求并发。
- 协议范围未变：只显示公开 API 返回的线路，未返回的后台线路无法枚举。

本次未上传或切换线上站点；真实 hub 安装和生产数据尚未在线验证。v2.0.0 包保留，可回退。
