# Monitor Next v1.5.0 第四阶段验收报告

日期：2026-09-19。结论：**地图、详情与国际化阶段完成，本地验收通过；真实 hub 部署与手机真机未验证。**

## 已交付

| 模块 | 功能 |
|---|---|
| 地图 | 悬停/键盘聚焦预览地区节点，节点详情入口，Enter/空格筛选，小地区标记与替代列表入口 |
| 地区统计 | 地区码大小写归一，空值/无效码归入未知地区；汇总与当前筛选列表数量一致 |
| 收藏 | 卡片、表格、详情均可收藏/取消收藏，支持仅看收藏；浏览器内保存节点 ID |
| 派生分组 | 系统筛选，卡片按地区或系统分组；状态在当前标签页保存 |
| 详情 | 上一/下一节点及下拉切换，筛选保留、深链刷新，统一面板外观 |
| 历史数据 | 手动刷新、失败重试、15 秒超时、节点切换取消旧请求；无记录/失败/超时区别显示 |
| 中英文 | 顶栏切换并保存语言；界面、时间/时长格式、地区名随语言变化；地区搜索支持中英文 |
| 兼容保留 | 五种卡片布局、四种指标、背景、费用估算及 v1.4.1 分钟级延迟刷新全部保留 |

语言切换不会翻译节点名称、站点名称、探测名称或用户备注。缺失翻译回退中文；插值数据在翻译后填充。该版本字典有 345 个词条，静态校验覆盖 358 处字面量翻译调用，变量标签通过界面测试核对。

## 测试与截图

- TypeScript、生产构建、代码检查通过。
- 八组单元检查通过：格式化、实时数据容错、延迟统计/缓存、浏览排序、外观迁移、计费边界、地区/收藏分组、翻译占位符/回退与语言格式。
- Chrome 完整回归 27/27，Edge 完整回归 27/27；最终均无失败、跳过或不稳定用例。
- 英文极窄屏设置面板最后调整为单列后，Chrome、Edge 分别追加英文完整界面场景 1/1 通过。
- 延迟刷新专项继续验证：首请求延迟 5 秒、连续两轮自动更新、手动缓存绕过、卡片/表格缓存共享、隐藏暂停及返回恢复。没有回退 v1.4.1 修复。
- 新增验证覆盖未知地区和小地区计数、键盘聚焦预览、地区节点跳转、收藏/分组刷新恢复、详情前后切换、失败重试、旧请求实际取消、关闭公开页跳转、禁用存储时的临时收藏与语言切换。
- 英文页面验证首页、外观、费用及规则、地图、详情的界面翻译；仅使用英文模拟节点数据时，main 内容不残留中文界面文案。
- 保留中文 80 种布局/图表/明暗/宽度组合及 30 种手机紧凑配色组合；新增英文 5 布局 × 3 宽度（320/390/1440px）检查，无整页横向溢出。
- 已人工查看英文地图、详情与最终手机设置截图。截图为模拟数据；面板裁图隐藏悬浮顶栏以免遮挡，生产页面保留顶栏。

## 数据与行为边界

地图上的标记是国家/地区代表位置，不是机房精确坐标；无法定位的节点始终可以通过未知地区列表找到。地区筛选和收藏筛选共同决定地图与节点列表范围；全局概览统计仍反映接口返回的全部节点。

收藏只在本浏览器保存 ID，没有新增后端写接口或共享标签；已经不在接口可见列表中的节点不会通过收藏显示。系统分组来自操作系统名称，不是管理员配置的业务标签。

语言、收藏和汇率与外观 JSON 分开存储；外观导入导出不会导出它们。恢复默认外观保留语言和收藏，重置全部偏好清空收藏/分组并回到中文。存储被禁用时仍能临时操作，但刷新后不保存。

切换语言保留详情地址和总览筛选，重建临时界面状态（例如详情标签/时间范围）。详情切换节点会重置图表状态，以免沿用另一节点的探测选择。公开资源历史最大 7 天、延迟最大 24 小时，实际拒绝或关闭公开页仍遵循后端返回。

## 性能和安装包

| 项目 | 结果 |
|---|---|
| 首页 JS gzip | 101.74 kB，相对 v1.4.1 的 90.63 kB 增长约 12.3%，低于 30% 目标 |
| CSS gzip | 12.26 kB |
| 按需模块 | 地图 253.55 kB gzip、详情 112.65 kB、费用 5.05 kB |
| 100 节点筛选 | 本机 Chrome 40.6ms，Edge 34.2ms，包含自动化开销 |
| 历史并发 | 最多 2，缓存与按需加载保留 |
| 压缩包 | 897,315 字节，低于 32 MiB |
| 解压文件合计 | 2,456,295 字节，低于 64 MiB |
| 最大单文件 | 781,336 字节，低于 8 MiB |
| 安装结构 | 根目录 theme.json、dist/、preview.png 和许可文件；不包含测试 API 或 node_modules |

SHA-256：`8749008a5f1dd58118feeabc578e4cb892af5459049e772213e1c047c7592949`。

性能为单次本地观测，不是所有设备的保证。主题短名继续为 monitor-next；后台上传新版安装包覆盖，源码 ZIP 不用于安装。无数据库变更，已保留 monitor-next-v1.4.1-baseline.tar.gz 可回滚。

## 未验证与下一阶段

本地 Windows Chrome/Edge 配合生产构建与模拟 API/WebSocket 验证；未连接用户的真实 hub、反向代理/CDN，未验证 Safari/Firefox 和手机真机，未对外发布 Release。下一阶段为安装体验与稳定发布，包含条件允许时的 PWA 评估，以及实际 hub 安装/升级/回滚联调；本次没有提前实现或宣称完成这些内容。
