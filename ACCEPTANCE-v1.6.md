# Monitor Next v1.6.0 — Lumina UI 实施与验收

日期：2026-09-19。按《LuminaPlus UI 借鉴分析》实施 UI-A、UI-B、UI-C 主线。

## 交付内容

1. Lumina 与原始风格可切换；浅灰/深色画布、圆角卡片、大数字总览、在线状态分段条、指标语义配色、资源双列分区、紧凑顶栏。
2. 线路标题、最新采样与 24h 丢包并列、状态色数值及分段历史；保留采样说明、缺样/超时/旧数据区分；点击历史曲线，不触发悬停开合。
3. 系统/地区胶囊筛选，保留多地区下拉；次要操作和排序继续折叠。
4. 桌面卡片布局与手机布局独立；既有配置继承旧布局，新增完整预设入口；原五种布局可用。
5. 节点线路覆盖本地保存，卡片、表格、延迟排序共用；丢失线路不自动替换。默认多线路展开可选，存储禁用仍可临时操作。
6. 当前筛选节点批量刷新、完成/失败计数、最多两个首页历史请求并发；后台暂停新请求派发，返回恢复。
7. 详情显示/隐藏全部线路、单线路可访问状态、真实桶数量与起止时间；超时和超过两小时的缺口断开。削峰继续默认关闭，启用时提示。
8. 页面渲染错误恢复入口，不暴露原始错误或节点数据。

## 验证结果

- 类型检查、生产构建、代码检查、七组基础单元检查通过；317 个字面量翻译调用覆盖通过。
- Chrome 整轮 31/31 通过；新增多线路/禁用存储和后台批量刷新检查后，相关 7/7 增量场景通过。合计覆盖 32 个独立场景。
- Edge 完整 32/32 通过，无失败、跳过或不稳定项。
- 新增验证：节点线路持久化和跨视图排序、消失线路明确显示、旧偏好迁移与手机布局、批量并发上限与部分失败/后台暂停、详情超时及长缺口分三段、渲染错误恢复。
- 旧延迟自动刷新、隐藏恢复和鼠标曲线稳定性回归继续通过。
- 中英界面 320/390/768/1440 宽度，五种布局×四种指标样式、明暗模式继续验证；额外检查 320px 快捷菜单展开无横向溢出。
- 已检查浅色首页、节点卡片、深色、手机尺寸预览。预览使用演示数据。
- 首页 JS gzip 101.61 kB，CSS gzip 13.76 kB，地图/详情仍拆包。

## 范围及安装

上传 monitor-next-v1.6.0.tar.gz 后，打开“外观设置 → 应用 Lumina 预设”获得完整新外观。这个按钮会清除背景图与毛玻璃并设置桌面经典/手机极简；仅切换视觉风格则保留其他偏好。

费用/资产估算未恢复；任意指标颜色编辑及额外动画按分析建议延期；后端管理接口不纳入。没有复制上游编译代码、字体或未明确授权的图标。

本次为本地模拟 API 与桌面浏览器验收，没有上传用户线上服务器，没有宣称完成真实 hub 或手机真机验证。
