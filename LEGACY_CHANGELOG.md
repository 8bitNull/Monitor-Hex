## 2.13.0 · 高负载记录

开启高负载提示后，总览新增第四栏；桌面四列，窄屏两列。逐节点记录 CPU ≥85% 的事件，低于 80% 恢复；展示开始时间、恢复/最后观测时间、持续时长和峰值。浏览器本地保留最近 100 条结束记录及正在进行的事件，恢复、刷新、关闭设置不删除历史。详情页停留期间继续监测；关闭页面期间无法监测，重开后旧的进行中记录标记监测中断，不伪造恢复或跨中断时长。存储不可用时明确提示。

## 2.12.0 · 详情页视觉精修

统一身份信息、Agent 后方备注标签与 V4/V6 状态；资源区增加负载，桌面四列、手机两列；上下行网速并排，统一图表留白、线路选中态和资料对齐。深色模式缩放条使用主题底色。

## v2.11.0 首页视觉精修

- 概览标题收敛，统计数字统一大小、字重与间距；趋势线降低视觉强度。
- 卡片统一标题、资源、网速、流量、连接数、线路和底栏节奏；缩小国旗、V4/V6 和状态标签，优先留给节点名称。
- 移除流量区的重复分隔线，其他分隔线柔化；深色减少阴影，正常状态与备注降低颜色强度，异常提示保留。
- 单线路隐藏与下拉框重复的线路名称，三条线路仍逐项标识。长标题可通过悬停查看完整名称。
- 地图浮动标题、工具、标签统一底色和阴影，删除地区小进度条，仅保留国旗、地区名称与数量；默认 220% 与交互保持。
- 构建、静态检查、数据/翻译检查通过；Chrome 与 Edge 相关各 9 项通过。1440/390/320px × 深浅两套主题检查长标题、协议标签、三线路、长备注与离线节点，无横向溢出或标题状态重叠。未部署线上。

## v2.10.3 协议标签位置

- V4 / V6 移到卡片在线状态左侧，合并成紧凑状态组，移除原独立行；缺少协议数据时不占位置。

## v2.10.2 卡片标题精简

- 删除首页卡片在线状态旁的图表图标，保留点击卡片进入详情。

## v2.10.1 线路下拉框修正

- 原生下拉选项显式使用主题背景与文字颜色，选中项使用主题柔和底色；明暗模式对应原生 color-scheme，修正“跟随全局”选项配色。
- 首页线路选择按内容宽度显示，最大 240px，不再撑满整行；长名称截断，悬停保留完整名称。
- 构建通过；Chrome / Edge 验证深色选项背景、文字色、控件宽度与键盘选择，320px 无横向溢出。尚未部署线上。

## v2.10.0 统一网速与延迟指标

- 首页总网速固定使用趋势线，删除相对峰值仪表及样式选择项；旧 gauge 偏好和导入自动归一为 spark。
- 首页卡片与详情页网速跟随全局指标样式：20 段柱条、细进度条、数值旁小圆环、极简数字。保留上行蓝、下行绿，圆环不另占一行。
- 延迟为真实历史采样，不设虚假的百分比上限：分段模式保留采样色条；进度条/圆环模式改为细连续色带；极简模式隐藏色带。详情历史曲线与首页总网速趋势线保留。
- 丢包指标与延迟左右对齐；圆环模式缩小间距，统一数值、单位、留白。网速指示仍为共享近期峰值比例，不代表带宽使用率；无数据继续显示 —，零值仍为 0。
- 构建、静态检查、数据与翻译检查通过。Chrome / Edge 本轮相关各 7 项通过；检查四种样式手机卡片截图和详情页继承。尚未部署线上。

## v2.9.3 地图浮动标签与视图切换

- 卡片模式且首页地图开启时，卡片/表格切换移入地图右下角；地图未显示时保留顶部切换。
- 地区筛选改为独立浮动标签，移除整条白色背景；手机标签与切换分行，避免遮挡。
- 修正地图通用 SVG 样式影响切换图标尺寸的问题。无其他筛选时不再保留空白顶部工具栏。
- 构建、静态检查通过；地图交互与地图/国旗切换两项回归通过，检查桌面与手机截图。尚未部署线上。

## v2.9.2 满幅地图与浮层

- 地图铺满面板，移除为标题与底栏预留的外部空间；保持默认 220% 居中缩放。
- 标题、统计、缩放与全屏工具、地区筛选、说明及节点摘要浮在地图上，使用轻透明底色保证可读性。
- 桌面地图面板 420px，手机 320px；手机工具纵向排列，底部地区横向滚动，避免遮挡标题。
- 构建及地图交互、地区显示切换两项浏览器验收通过，检查桌面与手机截图。未部署线上。

## v2.9.1 默认地图缩放

- 地图首次显示以画布中心放大到 220%；重新进入卡片地图或刷新页面同样采用此默认值。
- “适配全部”仍恢复 100% 全球视野，手动缩放和平移保持可用。

## v2.9.0 地图呈现与地区筛选联动

- 用国家轮廓高亮替代大数量气泡及偏移引线；小地区使用固定位置的小标记，数量放到地图底部国旗地区统计条。
- 显示地图时隐藏顶部国家、国旗与地区清除标签；关闭首页地图或切到表格时恢复国旗、地区代码、节点数量和所有地区图标。
- 地区筛选状态在地图、无地图卡片和表格之间保留，设置关闭地图后刷新仍保留。
- 保留拖拽、缩放、全屏和键盘操作。地区数量条的细条表示节点数量占比，不表示在线率。
- 构建、静态检查、数据与翻译检查通过；Chrome 本轮相关 8 项、Edge 地图与筛选 2 项通过。未运行依赖旧入口的历史全量浏览器测试，尚未部署线上。

## v2.8.0 可缩放节点地图

- 地图改为平移、缩放画布，桌面区域 380px、手机 230px；删除侧边常驻列表，使用地区数量圆点与短地区代码。
- 支持缩放按钮、适配全部、键盘方向键、原生全屏与退出。普通滚轮保留页面滚动；Ctrl / ⌘ + 滚轮缩放，全屏内直接滚轮缩放。
- 点击地区继续筛选卡片；放大到约 180% 后，选定地区展示最多六个节点名称和状态，其他节点在下方卡片查看。
- 数量标记区分在线、部分离线、全部离线；密集标记避让，辅助引线仅连接到所属地区，不表示网络连接。未知地区保留底部筛选入口。
- Chrome 与 Edge 本轮相关各 7 项通过；最后布局调整后地图交互复查通过。构建、静态检查、数据及翻译检查通过。未执行依赖已删除入口的旧全量浏览器测试，未部署线上。

## v2.7.1 筛选栏精简

- 删除全部、在线、离线按钮及地区文字按钮组；清除旧会话中的状态筛选，避免隐藏节点。
- 所有地区改为地球图标，点击清除地区筛选，保留悬停提示与无障碍名称。
- 地区筛选通过地图完成，手机地图恢复横向地区按钮，便于选择小地区和未知地区。卡片/表格入口保留。
- 构建、静态与数据检查通过；相关浏览器回归 6 项通过，另验证手机图标、地区清除和旧状态恢复。本轮未执行依赖旧筛选入口的全量历史浏览器测试。

## v2.7.0 地图融入卡片页

- 移除独立地图入口，视图切换只保留卡片和表格。
- 地图默认显示在卡片列表上方，与地区、在线状态及系统筛选联动；切换表格隐藏地图，返回卡片恢复。
- 去掉地图重复节点列表，桌面采用地图、地区在线数量和选中地区摘要；手机采用紧凑地图，复用上方地区筛选。
- 选中地区在地图上描边，支持键盘筛选；香港、澳门、新加坡及未知地区保留按钮筛选途径。
- 主题设置中的“首页地图”仍可关闭，并保存本地偏好；旧 map 字段不再强制切入地图独立视图。

## v2.6.0 首页卡片与多线路布局

- TCP / UDP 连接数常驻首页，过期及离线显示 —，有效零值保留 0。
- 首页线路默认 1 条，在主题设置的“线路 → 首页线路数量”中选择 1、2、3 条；选定线路优先，其余按现有顺序补足，不重复。超过三条的线路在详情页查看。
- 延迟区左侧为线路、延迟数值及近期采样色条，右侧为 24h 丢包百分比与指标；说明使用悬停提示，完整历史曲线在详情页。
- V4 / V6 标签单独一行，只有接口返回对应地址时显示；不展示地址本身，不把缺少数据视为不支持。
- 保持统一资源两列、上下行蓝绿配色、大数字小单位；有流量限额才显示进度条，在线与到期、备注保持紧凑底栏。
- Chrome 64 项、Edge 9 项通过；最后样式调整后 Chrome 6 项复查通过。构建、静态检查、数据及翻译检查通过。

## v2.5.0 实时状态与设置继承

- 卡片、表格、详情与实时汇总共用有效性判断：节点上报超过 60 秒，或浏览器超过 15 秒未收到有效节点快照，实时指标显示 —，恢复上报后自动恢复。离线、无数据和真实零值分别展示。
- 过期数据不计入实时总网速和高负载提醒；概览提示待更新数量，历史曲线及累计流量保留原有时间语义。
- 首页线路明确显示“跟随全局：线路名”；独立选择后可通过图标恢复跟随，其他线路仍在详情页查询。
- 浏览器仅保存与站点默认不同的设置，各首页模块独立继承。旧版偏好自动迁移；与升级时默认值相同的选项恢复继承，其余值保留。恢复默认外观后，外观继续跟随站点默认。
- Chrome 全量 62 项、Edge 相关 19 项通过；构建、静态检查、数据测试与 332 项翻译调用检查通过。本轮为本地验收，尚未部署到线上。

## v2.4.3 备注收敛与丢包指标

- 首页备注最多 3 个标签，单标签超过 24 字符显示摘要；通过 +数量或箭头进入详情查看完整备注。短备注原样显示。
- 首页 24h 丢包支持分段柱条、进度条、圆环与极简数字，跟随全局指标样式。按真实百分比填充，保留一位小数；无统计为 —，真实 0% 不混淆。
- 修正 320px 屏幕网速数值、单位、过期图标的拥挤。
- 已对实际站点的 21 个节点进行登录后只读体验，验证网速持续变化、详情线路、地区筛选和手机布局；未修改线上配置，验收后退出会话。

## v2.4.2 移除收藏与卡片备注

- 全主题移除卡片、表格及详情页收藏入口，删除收藏状态与样式；旧本地收藏数据不再读取或影响展示。
- 首页卡片原收藏区域改为后台备注，按中英文分号分成彩色标签，与详情页共用内容稳定配色。
- 费用与备注自动排布，手机备注换行，空备注不保留区域；长词可换行，纯文本呈现。
- 详情导航改为返回与节点切换两组布局，表格名称扩展到原星标空间。

## v2.4.1 网速展示统一与卡片精简

- 首页卡片与详情页共用上下行图标、数值、单位和细指示条；两条指示使用共享近期峰值，跨页面保留尺度，不冒充带宽使用率。
- 通过节点上报时间识别超过 60 秒的过期网速，淡化旧值并提示时间；零流量保留 0，离线和缺少指标显示 —。
- 网速样式移到首页模块，改名“首页总网速样式”；保留旧仪表偏好并明确“相对峰值仪表”，解释作用范围。
- 删除首页卡片采样信息和手动刷新按钮及采样说明区域；保留自动刷新、失败提示和详情页刷新。
- 构建、静态检查、数据测试、中英翻译检查通过；Chrome / Edge 各 54 项浏览器测试通过。

## v2.4.0 全站三轮体验优化

- 首页概览合并为紧凑区域，正常连接状态使用带提示的图标；手机页头压缩，状态/视图与横向地区筛选分为两行。
- 卡片移除 CPU 型号、架构和虚拟化辅助行（详情仍完整展示），上下行箭头化，未设到期不占首页位置，全站收藏统一星形图标。
- 延迟明确“近期趋势 / 24h 丢包”，单线路不显示数量入口，离线曲线标记历史数据。预留加载区高度，防止返回时位移。
- 表格延迟精简为数值和线路两行，刷新/历史为图标，资源为数字加细进度条；保留名称列固定、排序、列显隐。
- 返回总览和浏览器后退恢复节点焦点及滚动位置，切换节点保留资源/延迟标签。
- 详情恢复“平滑”“丢包”短标签，图例和批量操作分行，移除说明删除后的额外空白，手机设备资料紧凑双列，未设到期不再重复。
- 地图改为地图与地区节点简表，点击地区保持地图、更新列表，不再重复卡片；状态/系统筛选有效，小地区及未知地区仍可从列表选择。
- 主题设置改为桌面侧面板、手机全屏对话框，配色/指标/密度可视化选择，外观/首页/线路快捷定位，说明重置范围；支持 Escape、焦点限制及关闭后焦点恢复。
- 浏览器偏好兼容旧版；旧的首页地图配置可通过卡片/表格视图正常退出。

## v2.3.0 详情信息图形化

- 网络延迟统一稳定配色实线，图例悬停/键盘聚焦突出线路；超时与缺样继续断开。
- 移除采样桶与覆盖时间说明，窗口丢包提示移入图例提示；批量线路操作图标化。
- 资源图标、上下行箭头、系统/在线/到期图标取代重复前缀，图表切换与时间段统一分段控件。
- 月用量与配额合并，有限配额显示进度条，无限配额显示 ∞。设备资料保持完整。

## v2.2.2 轻量详情导航

- 移除导航外层卡片和独立按钮边框，左侧返回、中间集中切换节点、右侧收藏。
- 节点选择桌面限宽 300px，手机自适应；统一 36px 高度，保留键盘焦点、按钮名称和完整节点选项。

## v2.2.1 详情页备注标签

- 后台备注含英文或中文分号时拆分为标签，去除空白及空项，接在 Agent 版本后面；底部不再重复显示。
- 同一词组保持固定的柔和配色，适配明暗主题，长标签和手机布局自然换行。
- 不含分号的备注仍按原来的段落展示；备注始终作为纯文本渲染。

## v2.2.0 详情页优化

- 重排为紧凑标题、实时指标、历史图表、设备资料、流量与到期。资料常驻展开；CPU/内核等长字段完整换行。
- 返回总览、切换节点、前后节点和收藏合并到同一导航区域；前后与收藏使用有可访问名称的图标。
- 实时 CPU/内存/磁盘复用首页指标组件，上下行使用同一语义颜色，离线保持未知值。
- 资源/网络延迟标签、时间范围、刷新图标和平滑开关合并为一个工具栏。短时间标签保留完整可访问名称；标签切换同步 URL，刷新保留当前标签。
- 延迟页默认选中首页实际使用的线路（节点选择优先于全局）；失效选择明确无记录，不自动换线。多选、显示全部、隐藏全部、回到首页线路只影响详情比较，不修改首页偏好。
- 线路图例显示最新延迟、所选窗口丢包率及无采样/较旧状态，线路 ID 决定颜色/线型，跨时间范围保持稳定。采样覆盖区明确实际采样时间。
- “削峰”改为“平滑显示”，默认关闭并说明不修改原始数据。
- 延迟图表固定 360px，手机 280px；不再根据滚动位置计算高度。资源图表桌面两列、手机单列，设备与流量资料放在图表下方。
- 流量区显示后台已有的本月计费用量、配额、重置日、累计/今日流量和续费到期，不添加新接口。

## v2.1.1 页头精简

- 移除站点名称左侧的 M 图标及对应样式，保留名称、副标题和点击返回总览。

## v2.1.0 双列卡片与精简首页

- 卡片采用 2×2 资源区：CPU、内存、磁盘、1 分钟负载；图标与简短名称并列，分段条和数字保持告警语义一致。负载显示原值，条形按负载/核心数衡量压力，不把负载数字标成百分比。
- 上下行双列展示，速率突出、单位弱化；本月用量与配额单独一行；延迟仅显示一条选定线路和真实时间趋势。在线时长/到期置于底部，价格与收藏最后一行。
- 顶部单行标签保留虚拟化、架构和简短 CPU 型号；TCP/UDP、累计流量等继续在详情查看。
- 删除首页排序字段、升降箭头、分组、仅看收藏、重置排序、批量刷新延迟及相应第二排容器。状态、地区、系统筛选和视图切换保留。表格原有列排序与显示列仍可用，卡片按后台顺序显示。
- 全局主要探测线路移到右上角「外观设置」的「线路」区；写入主题偏好 probe，支持站点默认、导入导出和浏览器持久化。旧会话中的线路选择首次迁移；节点独立线路优先。
- 旧仅看收藏/分组状态不再影响展示；收藏标记本身保留。恢复默认外观不改线路选择，重置全部偏好恢复站点线路并清空节点独立选择。
- 自动刷新和单节点刷新保持；全局设置打开时在原有两请求并发限制下读取线路目录。API 未提供的线路仍无法凭空枚举。

## v2.0.0 统一视觉与一致性修复

- 总览、常驻筛选、卡片、表格、地图、详情和设置使用同一套颜色、字号、圆角与控件尺寸。桌面控件 36px，手机主要控件 44px；手机统计采用两列加通栏，避免缩小文字。
- 设置只提供舒适/紧凑密度、四种指标、配色和明暗等有效选项；退役旧皮肤/五套布局/独立手机布局。旧 skin/cardLayout/mobileLayout 仍可导入，但只作为兼容元数据，不再决定界面结构；没有密度的旧 compact 布局迁移为紧凑密度。导出不再写入退役字段。multiPing 完全移出偏好模型。
- 首次访问遵循站点默认；只有实际存在的早期保存配置执行既有一次性设计迁移。收藏、单节点线路与配色保持兼容。
- 资源百分比和图形共享告警颜色（75% 警告，90% 危险）；首页高负载提醒保持 85% 的原有阈值。上下行在首页与详情使用固定颜色，不跟随品牌配色变换。
- 线路目录合并服务端 probes 元数据与采样 ID，无采样线路仍能选择和在详情查看；显式选中后显示无记录，不自动换线。自动线路继续选第一条有采样的线路。
- 窗口丢包整表缺失/非法时显示未知；仅服务端给出合法 loss 映射而省略线路值时按协议视为零。单桶缺失丢包值显示未知，不伪造为零。详情长 CPU/系统字段允许完整换行。
- 节点级延迟订阅、按快照缓存摘要与目录、预计算排序键。缓存/快照共同淘汰，保留正在订阅/请求中的节点及最多 200 个其他快照。
- 新增危险状态、站点默认、无采样线路、未知丢包、长字段、触控尺寸与四张明暗/桌面/手机视觉基线。

后台未通过公开接口返回的线路无法由主题猜测；本主题只呈现实际收到的目录和记录。

## v1.9.0 独立主线路与紧凑卡片

- 首页每个节点只展示一条线路。点击线路名称从已有探测记录中选择，按节点保存于当前浏览器；默认跟随全局线路设置，全局自动时取节点首条记录。
- 选择失效或无记录时明确提示，不静默换线；选择跟随全局即可恢复默认。
- 图表图标与数量直达节点全部线路详情，使用 #latency 地址，刷新仍打开延迟页。
- 延迟排序和表格沿用节点独立选择；排序提示不同目标的可比性。
- 卡片保留简短 CPU 型号、架构、本月流量/配额；完整型号、负载、交换空间、累计流量在详情页。底部价格/到期/收藏合并一行。
- 线路区三行展示：名称与操作、波动/延迟/丢包、趋势图。采样时间放入信息按钮；旧记录、更新错误与超时直接提示。

## v1.8.0 常驻图形卡片

- 卡片分为资源指标、全部线路趋势、设备与用量三个常驻区域。
- 资源标签改为图标，保留百分比和容量；实时上下行紧凑排列。
- 所有线路直接显示最新延迟、24h 丢包、小型趋势图与波动；去掉卡片线路选择和多线路展开开关。表格与排序仍使用全局线路选择。
- 趋势图按采样时间绘制，超时红叉、超过两小时缺口断开。每条曲线独立缩放，信息按钮内可查看纵轴上限、颜色和统计口径。共享时间显示最新采样，不同时间和旧记录逐条提示。
- 去掉更多信息折叠，型号/架构、负载/交换空间、流量/配额与价格/到期常驻。不限量不显示空进度条。完整型号及指标说明可点击信息图标查看。
- 保留延迟自动刷新、手动刷新、错误提示、收藏以及现有外观偏好。旧 multiPing 偏好不再影响卡片，不再提供无效设置。

## v1.7.4 移除搜索并合并视图行

移除首页搜索框，卡片/地图/表格切换移到状态和地区筛选同一行右侧。窄屏自然换行。初始化时清除旧版保存的搜索词，避免无搜索框时仍过滤节点。

## v1.7.3 顶部导航精简

移除顶部“…”快捷开关及其状态和样式。后台入口常驻：登录用户显示“进入后台”，未登录显示“登录”。语言、外观和明暗切换保留。

## v1.7.2 常驻工具区精简

默认桌面改为三行：搜索与视图；状态与地区；排序、分组、线路及收藏/刷新操作。排序方向缩为相邻箭头，标签同行排列，移除独立操作行的大块留白。按钮文字简化，辅助功能名称继续说明完整操作。所有控件仍直接显示。多系统、多地区与窄屏按空间换行。

## v1.7.1 筛选区统一常驻

搜索、视图切换、状态、地区/系统、排序、分组、主要探测、收藏与刷新整合到一个面板。移除筛选折叠控件和折叠状态存储，所有控件直接显示；手机自动换行。节点卡片内“更多信息”和“采样信息”的展开方式不变。此调整取代 v1.7.0 的筛选收起策略，手机首屏优先完整展示筛选区，节点列表相应下移。

## v1.7.0 首页聚焦与渐进展开

- 默认总览仅显示节点、今日流量和实时网速；CPU 达到 85% 时额外显示高负载提醒，恢复后自动消失。
- 地区、系统、分组和排序统一放入“筛选与排序”；已启用条件在外部显示，可逐项清除。
- 节点卡片默认保留身份、三项资源、实时网速和主线路延迟；硬件、负载、累计流量、月用量、价格与到期日收进“更多信息”。详细卡片布局仍默认展开这些信息。
- 延迟标题直接作为线路选择入口，默认不重复线路名称；波动、采样时间和色彩说明统一收进“采样信息”。错误、旧记录和超时继续直接显示。
- 手机总览三栏并排，390×844 首屏可看到首个节点资源、网速和延迟色带；展开筛选不造成横向溢出。
- 保留现有布局、偏好、收藏、地图、表格、延迟自动刷新与点击展开历史曲线行为。

以下版本条目为历史记录；与新版不同之处以 v1.7.0 为准。

## v1.6.2 首页继续收敛

- 状态筛选仅保留名称，不重复总览数量；仅筛选结果少于总数时显示“显示 x/y 个节点”。
- 收起的筛选与排序并入状态行，标题/更新时间在桌面合并一行；降低总览留白。
- 仅有一种系统且未启用系统筛选时不显示系统按钮组，已启用条件仍可清除。
- 去掉重复在线色条；网速仪表不再同时显示第二份合计。
- 分段布局 CPU 下方显示处理器型号，负载在独立区域显示；5/15 分钟辅助值不重复一分钟主值。
- 未设到期日时不显示卡片占位标签，详情仍可查看到期信息；已知到期、过期和异常提醒保留。

## v1.6.1 默认外观与筛选整理

新版默认直接使用新外观，不再需要应用预设。页面移除了上游风格名称及预设按钮，外观选项显示“默认风格 / 原始风格”。升级时对没有 designVersion 标记的旧浏览器偏好执行一次迁移：启用默认风格和分段指标；配色、背景、卡片布局、语言与收藏继续保留。迁移后手动修改指标样式会正常保存，不会每次刷新重置。

筛选面板上排为排序、方向、卡片分组、主要线路；下排为收藏、表格列设置、恢复排序与刷新。移除重复系统下拉框；搜索下方离线按钮后的地区下拉框已删除，地区标签栏作为唯一地区筛选入口。原始风格也能使用系统/地区标签。

## v1.6.0 Lumina 外观与交互

- 新增 Lumina / 原始风格切换。Lumina 使用浅灰画布、圆角白卡、大数字总览、在线分段条、系统/地区胶囊、彩色资源分段条和负载区。
- 延迟区参考 LuminaPlus 重新实现：线路标题、并排延迟/丢包色带、按状态着色的数值、缺样留空，明确最新采样与 24h 汇总；历史曲线继续点击展开。
- 每节点独立选线路，与表格及延迟排序共用；可恢复跟随全局。线路消失时显示无记录，不自动换线。支持默认展开多线路。
- 手机布局独立设置；旧配置未指定手机布局时继承原卡片选择。顶栏收起后台入口，保留外观、语言、明暗操作。
- 在“筛选与排序”中批量刷新当前筛选节点，显示完成/失败数。共用两并发请求队列，页面后台暂停派发，返回后继续。
- 详情增加全部线路显隐、采样桶数与时间范围，超时及长缺口不连线。增加页面渲染错误恢复入口。

### 升级后切换完整预设

上传新版主题包后，打开 **外观设置 → 应用 Lumina 预设**。会切换 Lumina 风格、分段资源条、桌面经典/手机极简布局，并清除背景图和毛玻璃。单独切换“视觉风格”仅更改外观，不清除其他偏好。现有偏好不会在升级时强行重置；原始风格和五种布局仍可选。

每节点线路仅保存在本浏览器 monitor-next-node-probes-v1 中，不包含探测目标地址；禁用存储时仍可临时使用。“重置全部偏好”清除线路覆盖，“恢复默认外观”保留它。外观导入/导出不包含节点选择。

颜色表示指标类别或采样状态，不代表服务质量评级；分段条是数值比例，负载条按单分钟负载/核心数归一。在线分段条最多绘制 60 个节点作为预览，在线/离线总数始终统计全部节点。

源参考为 LuminaPlus 8284ebf95f0c9163dcce72e933bad9ebc28b4f39，当前 TypeScript 工程重新实现设计，未复制其压缩 JS、字体或第三方图标。费用/资产估算继续排除。逐指标任意颜色编辑和额外动画按原建议延期，保留既有主题配色。

# v1.5.3

- 修复历史曲线悬停时反复展开/收起导致的上下跳动；改为点击切换，支持键盘和触屏。
- 增加鼠标反复移入/移出后卡片高度稳定的回归检查。

# Monitor Next v1.5.2：界面收敛

- 标题、连接状态与更新时间合并，移除装饰性英文标题和宣传副标题。
- 搜索、视图、状态和地区筛选放入同一浏览面板；收藏、系统、分组、排序、主要线路、表格列选项收进“筛选与排序”。
- 折叠栏显示启用的高级条件，开合状态在当前标签页内保存；浏览器禁用存储仍能使用。
- 延迟采样时间、读取时间与颜色说明放入可点击的“采样信息”；旧记录及读取失败提醒仍直接显示，历史曲线独立可用。
- 经典卡片缩小圆环和间距，移除悬停上浮。自定义背景下不再为每排工具单独绘制底板。


# v1.5.1

- 按用户要求撤销阶段 3：移除费用面板、费用与剩余价值计算、手动汇率、专用样式、翻译和测试。
- 保留原有节点字段与 v1.4.1 延迟刷新修复、v1.5 地图/详情/国际化/收藏。
- 以下记录为历史版本变更，不代表当前仍提供费用功能。

# 更新记录

## 1.5.0 — 2026-09-19

- 地图支持地区节点预览、键盘操作、小地区补充标记和未知地区统计，地区码统一处理大小写。
- 新增本地收藏、仅看收藏、系统筛选及地区/系统卡片分组。
- 详情支持当前筛选内的节点切换、历史刷新/重试、超时与过时请求取消；统一面板风格。
- 新增简体中文/英文界面、语言保存、格式随语言切换及中英文地区搜索；用户数据保留原文。
- 调整手机顶栏与英文长文案布局，保留 v1.4.1 延迟刷新修复。

升级：后台上传新安装包覆盖 Monitor Next。回滚：上传保留的 v1.4.1 基线或切换默认主题；没有数据库变更。新增语言和收藏使用独立本地存储键，旧版会忽略它们。


## 1.4.1 — 2026-09-19

- 修复首页延迟请求粒度过粗：24h 窗口改为 1440 点预算，获取分钟级桶，仍显示最近 40 桶和完整 24h 丢包率。
- 修复刷新定时器与缓存到期时间错位导致隔轮不请求；改为请求完成后再调度。
- 卡片/表格新增手动刷新与最近读取时间，手动刷新绕过已完成缓存但合并进行中请求。
- 历史请求禁用浏览器 HTTP 缓存，保留可见加载、后台暂停和并发限制。
- 新增延迟响应、连续两轮自动更新、手动刷新、共享缓存及后台恢复回归测试。


## 1.4.0 — 2026-09-19

- 新增按当前筛选范围的套餐费用面板，各币种分别汇总月均、年均、剩余价值和一次性标价。
- 使用日历月与闰年规则估算周期，明确列出零价、缺失字段、过期、无到期日及到期超过一个周期的情况。
- 新增到期明细筛选、节点详情跳转和自动跨日更新。
- 新增手动汇率及日期校验、历史汇率提示、本地保存与清除；完整汇率缺失时不生成合并金额。
- 费用模块按需加载，保留 v1.3 外观和 v1.2 延迟、表格等功能。

升级：上传新版安装包，短名仍为 monitor-next。回滚：上传保留的 v1.3.0 包或切换默认主题，无数据库变更。手动汇率与外观导出分开保存。


## 1.3.0 — 2026-09-19

- 新增五种卡片布局、四种资源指标样式，布局与密度独立配置。
- 新增背景预设/自定义图片、遮罩与模糊、卡片透明度和毛玻璃；图片失败回退、手机模糊上限及减少透明效果支持。
- 新增首页模块开关、地区与时钟模块、相对峰值网速仪表盘、国旗/系统图标和容量总值显隐。
- 新增跟随系统明暗、偏好 schema v2 迁移、导入导出、分别恢复外观与全部偏好。
- 新增随包 theme-config.json，支持全站默认外观和访客本地覆盖。
- 保留 v1.2 的表格、排序、探测选择、延迟历史及连接回退机制。

升级：后台上传 v1.3 安装包覆盖原主题。回滚：切换默认主题或上传保留的 v1.2 包；无数据库变更。外观偏好可先导出留存。


## 1.2.0 — 2026-09-19

- 新增节点表格、列显隐、固定名称列和手机横向滚动。
- 新增多字段排序、缺失值置后和恢复后台默认顺序。
- 增加中文地区搜索，保存当前标签页中的筛选/排序/视图及探测偏好。
- 新增主要探测线路选择、其他线路展开、历史曲线和采样时间提示。
- 共享延迟缓存和并发队列；延迟排序读取进度可见。
- 改进连接状态，区分实时/轮询/断开，增加静默连接看门狗与请求超时。
- 保留现有配色、地图、套餐展示和节点详情。

升级：在 monitor 后台上传新版 theme.tar.gz，选用 Monitor Next。回滚：切回默认主题或上传保留的 v1.1.0 包；本版本没有数据库变更。

验证边界：本地真实 Chrome/Edge 浏览器配合模拟 API；尚未在用户的实际 hub、反向代理或手机真机环境联调。




