# v0.0.14 视觉稳定性与异常体验验收

2026-09-21。用户授权实施、验收后直接发布。基线为 v0.0.13（ff3615c）生产构建，使用固定演示数据对比。

## 实施与取舍

- 桌面采用同排等高卡片，辅助信息区向底部对齐。正常、离线、长备注和到期信息混排的截图中，额外空隙主要来自离线历史提示与不同页脚内容，未出现大面积空白，因此保留方案。手机不强制等高。
- 网速数字保留原有字号和等宽数字，单位独立占位；320px 屏幕采用固定分行。未知值保持占位，真实零值仍显示 0。移除与节点状态重复的网速过期图标，保留文字状态与时间提示。
- 首页与详情采用静态结构化加载占位。请求失败、无记录、等待首次上报、离线历史、数据过期、探测超时保持不同表达；合并重复的失败提示。
- 详情同节点、同类型、同范围刷新失败继续显示上次成功数据，并给出错误和重试按钮。更换范围或类型时不借用不匹配的图表。恢复成功后提示消失。刷新期间禁用按钮并同步阻止重复调用；取消和活动请求校验阻止旧响应覆盖。
- 从详情返回首页时优先定位原节点并恢复焦点；未改变宽度时尽量恢复原偏移，跨宽度返回时放在顶栏下；节点消失则回退原滚动位置，由浏览器限制到当前可滚动范围。短时布局观察用于处理返回初期变化，用户滚动或操作即停止修正。
- 原筛选、视图、排序状态继续由首页组件保留；直接进入详情没有首页定位记录，返回从顶部开始。未改变实时排序的主排序或既有稳定次级顺序。
- 没有新增偏好字段。已有手机方案、列数、辅助信息开关与外观偏好兼容。设置面板滚动锁定和关闭焦点已有实现，经回归通过后保留。

## 验收结果

| 检查 | 结果 |
| --- | --- |
| 静态检查、业务测试、生产构建 | 通过；390 处字面量翻译检查通过 |
| Chrome 完整回归 | 71/71 通过，含新增稳定性 13 项 |
| Edge 相关回归 | 27/27 通过：稳定性、导航、详情与设置、默认继承和过期数据 |
| 布局宽度 | 320、390、430、720、721、1024、1440、1920px |
| 节点数量 | 1、6、30、100，各宽度均无页面横向溢出；桌面同排高度差小于 1px |
| 返回定位 | 390/1440px 原节点位置误差不超过 24px；浏览器后退、页面返回及宽度交换通过 |
| 列表变化 | 节点重排后仍找到原节点；节点删除后回退有效滚动位置 |
| 动态单位 | 320/1440px 下 B/s、KB/s、MB/s、GB/s 切换高度不变，无数值溢出，数字继承正确字号 |
| 刷新与竞态 | 刷新中保留同范围图表；失败后曲线不变；重复调用仅一个请求；重试恢复；旧范围迟到响应不覆盖新范围 |
| 加载、超时、空数据 | 加载占位、15 秒请求超时后的错误提示、成功空记录分别呈现 |
| 状态与极端数据 | 真实零值、离线、过期、首次等待、请求失败、探测超时及长名称/备注/金额通过现有回归与截图检查 |
| 自定义兼容 | 64 种辅助信息组合、手机独立/跟随、720/721 边界、列数、导入导出与重置回归通过 |
| 视觉组合 | 既有用例覆盖中英文、深浅色、四种指标样式；地图、表格、地区、告警继续通过 |

以上是分层覆盖，未将所有维度做笛卡尔积测试。100 节点验证布局和交互可用性，不是低端手机帧率或压力性能认证。

## 视觉证据

- [桌面浅色前后对比](screenshots/v0.0.14/compare-1440-light.png)
- [桌面深色前后对比](screenshots/v0.0.14/compare-1440-dark.png)
- [手机浅色前后对比](screenshots/v0.0.14/compare-390-light.png)
- [手机深色前后对比](screenshots/v0.0.14/compare-390-dark.png)
- [六种数据状态](screenshots/v0.0.14/states.png)
- [失败后保留历史图表](screenshots/v0.0.14/history-retained.png)

视觉检查发现数字包装元素继承了旧的小字规则，已修复并加入字体检查后重新构建、完整回归和截图。前后图由 scripts/capture-stability.mjs 生成；原始基线和完整截图位于本地 tests/artifacts/stability，不随安装包发布。

## 发布与边界

版本 0.0.14，短名 hex，更新地址 https://github.com/8bitNull/Monitor-Hex。发布附件为 theme.tar.gz 与 theme.tar.gz.sha256。

检查使用本地演示 API 与模拟故障。手机使用 Chrome/Edge 视口模拟，未测试实体手机、iOS Safari 或线上真实后端部署。首页返回定位只保存在当前页面会话；浏览器整页刷新不承诺恢复之前节点位置。
