# v2.4.1 验收记录

完成设置范围梳理、首页卡片与详情页网速统一和网速数据状态规范，并删除首页卡片的采样信息及刷新按钮。

## 行为

- 上下行以方向图标、数值、单位和 3px 细指示条表示。共享最近 60 次有效快照中单方向最大速率，内存缓存在首页与详情页之间保留，离线后清空。
- 不显示带宽占用百分比；CPU、内存、磁盘仍使用真实占用比例。
- 上报超过 60 秒的网速显示时钟标记、淡化，并提供上报时间提示；每 10 秒检查。缺少指标和离线为 —，真实零值为 0。
- 首页总网速样式只控制首页概览；原 gauge / spark 偏好兼容。仪表仍是近期相对峰值，不是带宽容量。
- 卡片删除两个按钮及采样说明；每分钟自动刷新、失败提示、表格/详情页刷新保留。

## 验证

- npm run build、npm run lint、npm test 通过。
- Chrome：54 passed，32.8 秒。Edge：54 passed，50.5 秒。
- 自动测试覆盖相对尺度、首页详情一致性、零值/缺失/离线/过期转换、设置范围和保存、按钮移除、自动刷新与请求并发。
- 已检查桌面首页和详情页，以及手机浅色/深色卡片截图；320、390、768、1440 像素宽度相关回归通过。

仅本地演示与模拟接口验收，未连接或部署到生产站点。详情页导航与其他非本轮重点功能未改动。
