# Monitor HEX 0.06 落实与验收

验收日期：2026-09-20。范围：本地源码、演示数据、Chrome/Edge 浏览器和主题安装归档；未部署线上站点。

## 第一轮：详情页视觉与导航
- 删除独立白色节点切换条及前后箭头，将切换入口合并到节点标题。
- 桌面采用标题下浮层，手机采用底部面板；节点超过 8 个时提供搜索，显示国旗、在线状态及当前节点。
- 支持 Escape 关闭、焦点回归；切换节点保留资源/延迟页签、时间范围与线路选择，不改动首页线路设置。
- 沿用统一色彩、边框、圆角和指标样式；CPU 完整名称可复制。

## 第二轮：首页到详情页的查询路径
- 卡片主入口进入资源历史；点击线路延迟数值直达该线路历史。
- 高负载记录中的节点可进入 CPU 历史，并显示事件起止时间；按事件时间选择可用历史范围。
- 超过 7 天的事件提示范围不能完整覆盖；无对应历史样本时明确提示。已删除节点入口禁用。
- 地图新增“恢复默认位置”，恢复 169% 和既定平移；“适配全部”仍单独保留。
- 表格首列固定、数字右对齐；首页线路选择弱化背景，减少视觉干扰。

## 第三轮：兼容与交付
- 历史刷新按钮提示最后成功获取时间，失败不会伪装成数据已更新。
- 标准版本更新至 0.06，保留原设置存储键及既有偏好。
- 生成桌面/手机浅深色首页、详情和延迟截图，更新安装预览。

## 验收证据
- TypeScript 与生产构建：通过。
- 静态检查：通过。
- 9 组业务单元测试与 361 处文字翻译覆盖检查：通过。
- Chrome：25/25；Edge：25/25。记录：hex-v0.06-chrome.txt、hex-v0.06-edge.txt。
- 浏览器用例覆盖：节点搜索、切换状态保留、关闭后的焦点、精确线路跳转、告警历史入口、失败刷新时间、复制、地图复位、表格流量与到期、离线/过期数据、四种指标布局、深浅色及窄屏。
- 响应式详情截图尺寸：320、390、768、1024、1440 px。人工检查了桌面浅色详情、手机深色详情及手机节点选择面板。

## 交付与限制
- 安装包：monitor-hex-v0.06.tar.gz，根目录直接包含 theme.json 与 dist，附带预览和许可文件。
- 源码：monitor-hex-v0.06-source.zip；校验：monitor-hex-v0.06.tar.gz.sha256。
- 历史可用范围依赖后端实际保留数据；主题不会补造历史样本。
- 高负载事件仍是浏览器本地记录，仅能记录页面监测期间观察到的事件，不等同于服务端持续告警服务。
- 截图使用本地演示节点，不是线上真实监测数据；线上安装后的实际数据与后端兼容性仍需部署后观察。
