# Monitor Next v1.3.0 第二阶段验收报告

日期：2026-09-19。结论：**完整外观与默认配置阶段开发完成，本地验收通过。真实 hub 安装、更新和手机真机未验证。**

## 交付功能

| 项目 | 实现结果 |
|---|---|
| 五种卡片布局 | 经典、现代、极简、详细、紧凑行；密度另行配置，紧凑行可展开延迟历史 |
| 四种指标样式 | 圆环、进度条、分段柱条、极简数字；保持同一份真实指标口径 |
| 背景与毛玻璃 | 内置山峦、自定义图片、遮罩/模糊/透明度；图片失败与超时回退；手机模糊上限与减少透明效果支持 |
| 首页模块 | 在线、最忙、流量、网速、地区、时钟、地图独立开关；网速可选相对峰值仪表盘 |
| 其他外观 | 六种配色、系统/浅色/深色、国旗/系统图标、容量总值显隐 |
| 配置 | 随包公开 theme-config.json；访客本地覆盖；旧版偏好迁移；JSON 导入导出；分别恢复外观与全部偏好 |
| 原功能回归 | 表格、排序、筛选、探测选择、24h 延迟/丢包、历史、地图、详情和连接回退均通过已有测试 |

该阶段没有修改后端或数据库，不含下一阶段费用估算和汇率功能。

## 验收证据

- 代码检查、TypeScript、生产构建：通过。
- 五组单元检查：格式化、实时指标容错、延迟统计/缓存/并发、排序/搜索、外观配置校验与迁移：全部通过。
- Chrome：17/17；Edge：17/17。最终两轮均无失败、跳过或不稳定用例。详细 JSON 报告随交付提供。
- 每个浏览器覆盖 5 布局 × 4 图表 × 2 明暗 × 2 宽度（320/1440px），共 80 种组合；检查核心值可见且一致、无整页横向溢出。
- 每个浏览器另覆盖 6 配色 × 5 布局，共 30 种 390px 紧凑密度组合；检查配色不同、身份标识可见、无整页横向溢出。
- 背景成功、404 回退、清除/替换，移动端模糊限制与系统减少透明效果分别验证；生产 CSS 优化曾丢失标准毛玻璃声明，调整声明顺序后重新构建并通过测试。
- 全站默认值、本地覆盖、旧偏好迁移、明暗系统变化、导入导出往返、非法 JSON/版本/地址拒绝、两种重置范围，以及存储禁用均已验证。
- 五种卡片、桌面预览、设置面板与手机长图已人工查看。截图为本地模拟节点数据，不是用户生产节点。

## 性能与包校验

| 指标 | 本地观测 |
|---|---|
| 主 JS gzip | 90.30 kB，相对 v1.2 的 85.95 kB 增长约 5.1% |
| CSS gzip | 10.92 kB |
| 100 节点筛选 | Chrome 41.0ms，Edge 32.0ms，包含自动化开销 |
| 历史并发 | 最大 2，保留共享缓存与可见加载机制 |
| 压缩包 | 1,052,546 字节，低于 32 MiB |
| 解压文件总量 | 2,582,131 字节，低于 64 MiB |
| 最大单文件 | 779,969 字节，低于 8 MiB |
| 包结构 | 根目录 theme.json、dist/、preview.png 和许可文件；含 dist/theme-config.json；未包含演示 API 或 node_modules |

安装包 SHA-256：`9e0893f318f29c1b746718b8a86313c46460563b0f771c4f4e1a3a0cb11b80ee`。

性能数字是单次本机观测，不代表手机性能保证。自定义背景图片的网络体积由图片提供者决定，建议自行压缩；本版仅限制模糊强度、加载超时与地址格式，不提供图片压缩服务。

## 安装、配置与回滚

后台主题页面上传 monitor-next-v1.3.0.tar.gz（或同内容 theme.tar.gz），选择 Monitor Next。不要上传源码 ZIP。主题短名仍为 monitor-next。安装说明包含可修改的默认配置字段和优先级；现有访客偏好优先于站点默认值，可通过重置采用新的默认值。

升级前可导出当前外观偏好；如需回滚，切换默认主题或上传保留的 monitor-next-v1.2.0-baseline.tar.gz。旧版本只使用它识别的偏好字段。

验收环境：本机 Windows，已安装 Chrome 与 Edge，生产构建配合模拟 API/WebSocket。未连接用户的真实 hub，未验证实际反向代理、Safari/Firefox 或手机真机，也未对外发布 GitHub Release。真实部署联调仍属于后续发布验收范围。
