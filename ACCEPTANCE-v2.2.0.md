# v2.2.0 详情页验收记录

2026-09-20，Windows / 本地模拟 API。

- TypeScript/Vite 构建、oxlint、8 组单元与数据检查通过；304 处字面翻译调用覆盖通过。
- Chrome 与 Edge 全量回归各 48/48 通过（19.5 秒 / 45.6 秒）。
- 详情页按实时指标、历史图表、设备资料与流量信息重新排列，统一导航、控件和明暗主题样式。
- 默认线路继承首页全局设置和节点覆盖；详情页多选对比不会修改首页设置。指定线路无记录时明确提示。
- 历史图表固定高度；悬停、平滑显示、隐藏全部线路不改变图表高度。平滑仅改变显示，默认关闭。
- 图例显示最新采样值和所选时间范围的丢包率，保留采样覆盖信息、超时及长时间缺口。刷新后保留当前图表标签。
- 320/390/768/1440 宽度验证无横向溢出；离线实时指标不显示过期数值；超长节点名称和 CPU 信息正常换行。
- 已查看桌面浅色、深色及手机详情页截图；首页既有四组视觉基线回归通过。
- 首页、地图、表格、持久化设置、自动刷新、错误恢复、中英文和并发上限回归通过。

本次仅本地验收和打包，未上传或切换线上站点。真实 hub 安装与生产数据尚未在线验证。旧版本包保留以便回退。
