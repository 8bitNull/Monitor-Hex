# v2.4.0 三轮优化验收记录

2026-09-20，Windows，本地模拟 API；未部署生产站点。

## 交付范围

1. 首页：概览合并，正常连接信息降噪，手机页头收紧，状态与视图同排、地区横向滚动。表格延迟两行、资源细进度条、名称列固定、图标列设置。恢复首页滚动与节点焦点，切换节点保留图表标签。
2. 卡片与详情：精简重复硬件资料、上下行图标化、统一星形收藏，未设到期不占首页；近期趋势与 24h 丢包分开表意，离线标记历史数据，单线路不重复数量按钮。详情保留“平滑/丢包”短标签，批量操作归入工具栏，删除多余高度，移动端设备资料紧凑排列。
3. 地图与设置：地图配地区节点简表，不重复卡片，支持状态/系统/地区筛选。桌面设置侧面板、手机全屏，配色/指标/密度可视化，分类快捷定位，重置范围说明、焦点限制、Escape 关闭及焦点恢复。

## 验证结果

- 构建和 lint 通过，无警告。
- 8 组单元与数据检查通过；337 处字面翻译调用覆盖通过。
- Chrome 全量 52/52 通过（20.5 秒）；Edge 全量 52/52 通过（46.2 秒）。
- 最后将线路批量操作移入工具栏后，Chrome/Edge 各 13 项相关回归再次通过。
- 新增四项验收覆盖手机首屏、表格行高/历史跳转、返回与后退的滚动/焦点、地图简表筛选及旧首页地图偏好退出、设置键盘限制/关闭/持久化。
- 专项重复运行 3 轮，共 12/12 通过。加载时预留延迟区域，解决并发回归发现的首页返回位置偏移。
- 390×844 默认首页：首卡顶部约 383px，主要资源区底部约 609px；默认表格最大行高 63px。
- 320/390/768/1440、中英文、明暗、两种密度、四种资源样式、六种配色、旧偏好迁移、禁用存储、地图未知/小地区、请求失败及恢复、并发上限回归覆盖。
- 四张卡片视觉基线更新并在两浏览器验证。保存首页、手机、表格、地图、设置、深色详情及手机详情预览。

## 边界

真实 hub 安装及生产数据未在线验证。筛选/设置仅使用本地模拟站点验收；API 协议未更改。旧版本安装包保留，可回退。
