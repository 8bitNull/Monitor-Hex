# v0.0.13 显示内容自定义验收

2026-09-21。用户已授权验收后直接发布。

## 实施

- 卡片信息六开关：本月用量、TCP／UDP、在线时长、到期信息、备注、价格。隐藏后无空行或孤立分隔线，详情页始终保留完整资料。
- 手机显示默认跟随通用设置。第一次独立设置复制当前通用选择；切回跟随后保留手机方案，再次启用恢复。720px 及以下使用手机方案，721px 及以上用通用方案。
- 桌面列数默认自动，可选 2／3／4；手动模式使用 300px 最小卡片宽度，窄窗口自动降列，空余列保留以避免少量节点过度拉伸。手机单列。
- 高级外观默认折叠。主要控件继续使用至少 44px 点击高度。
- 新字段支持旧配置回退、站点默认、显式选择、导入导出与恢复。恢复外观保留显示配置，全部重置重新跟随站点默认。

## 验收结果

| 检查 | 结果 |
| --- | --- |
| 静态检查、业务测试、生产构建 | 通过 |
| 翻译覆盖 | 388 处字面量通过 |
| Chrome 完整回归 | 58/58 通过 |
| Edge 自定义、继承、语言 | 18/18 通过 |
| 六开关组合 | 全部 64 种通过；空行、价格/备注、时长/到期互斥正确 |
| 尺寸 | 320、390、430、720、721、1024、1440、1920px |
| 列数适配 | 每个尺寸检查中英文、深浅色、四种列数选择及四种指标样式；无横向溢出，资源数值不越界 |
| 手机方案 | 首次复制、独立修改、跟随切换、刷新恢复、边界切换与两种重置通过 |
| 配置兼容 | 旧配置补默认、非法值回退、明确选择恰等于默认值时仍保留、未修改项跟随站点、新配置导出再导入通过 |
| 默认视觉 | 390px 与 1440px × 深浅色四组，节点卡片截图与 v0.0.12 基线逐字节一致 |
| 既有功能 | 地图、地区筛选、卡片/表格、线路、详情、告警与语言全量回归通过 |

配置业务测试另验证独立手机方案完整导入导出及恢复外观保留显示字段。测试与截图均使用演示数据，不代表线上真实节点。

## 预览与边界

- [默认手机卡片](screenshots/v0.0.13/card-default-light.png)
- [手机精简示例](screenshots/v0.0.13/card-mobile-slim-light.png)
- [桌面四列](screenshots/v0.0.13/desktop-four-columns-light.png)
- [手机显示设置](screenshots/v0.0.13/settings-mobile-light.png)

同目录提供深色版本。基线为实现前保留的 v0.0.12 本地生产构建；固定数据截图及原始基线保存在本地 tests/artifacts/customization，不提交仓库。

手机验证采用浏览器视口模拟，未进行实体手机、iOS Safari 或线上部署验收。偏好为当前浏览器本地保存，不跨设备同步。

## 发布

版本 0.0.13，短名 hex，更新地址 https://github.com/8bitNull/Monitor-Hex。附件 theme.tar.gz 与 theme.tar.gz.sha256。
