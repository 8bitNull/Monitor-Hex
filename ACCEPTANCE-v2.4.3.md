# v2.4.3 验收记录

## 真实站点观察

2026-09-20 对用户指定站点完成登录后的只读体验，21 个节点在线。未登录访问会跳转到后台，公开前台接口标记为关闭；没有修改后台配置。验收后退出了本次登录会话。

- 实时上下行数值和指示条持续变化。选择一个节点进入详情后网速状态为 live，历史资源与三条延迟线路可正常加载。
- 桌面卡片高度约 554–589px，备注多数为 2–3 个短标签；个别线路说明偏长。
- 390px 手机宽度首页/详情没有横向溢出，首张卡片资源指标底部约 624px。离线筛选正确显示 0/21 的空状态，日本地区正确显示 2 个节点。最后恢复全部节点和所有地区。
- 无依据要求替换现有网速图形，因此保留其相对峰值定义。本次未部署新版本到站点。

## 本地改进

备注最多显示三个短标签，24 字符以上显示摘要，完整内容进入详情查看；不修改原始备注或颜色规则。
丢包图形随全局四种指标样式切换；进度按 0–100% 实际值显示，分段采用部分填充保留低丢包比例，不将 0.1% 画成整段 5%。未知数据与真实零值分开。
320px 网速区域减小字号和间距，单位与过期图标不再重叠。

## 测试

构建、静态检查、数据与翻译检查通过。Chrome 全量 58 项通过（26.7 秒）；Edge 本轮相关 8 项通过（20.0 秒）。最后 320px CSS 调整后，Chrome 网速/备注/丢包相关 5 项通过（16.6 秒）。
检查手机丢包与备注截图、浅色/深色卡片基准。测试涵盖 0%、7.7%、100%、未知丢包、四种指标样式、长备注摘要与完整文本、网速突增回落和跨页保持尺度。
