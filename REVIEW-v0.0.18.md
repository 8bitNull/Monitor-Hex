# v0.0.18 详情页比例与图表精修验收

2026-09-21。基线 v0.0.17（91a431b）。用户授权实施、验收通过后直接发布。

## 实施与取舍

- 桌面绘图区从 440px 调整至 360px；减少详情顶部、图表边距与控件间距，保留文字大小与操作面积。左侧实时概况保持自然高度。
- 1200px 起资料采用 1.2∶1.1∶0.8 列宽，硬件和网络得到更多空间，费用卡片不再被拉高；900–1199px 硬件、网络并排，费用位于完整底行。手机顺序与折叠偏好保留。
- 提示框标题行包含时间和手机关闭按钮，表体为名称/数值两列，长名称换行；延迟最多一位小数，0 保留，缺失数据继续按既有逻辑处理。关闭按钮不随列表滚动。
- 提示框根据所属绘图区宽度收缩。测试不仅检查屏幕边界，也检查绘图区裁切和关闭按钮完整可见。手机仍通过点击取点，桌面悬浮交互保留。
- 线路 ID 固定映射到五色与四种线型，图例与曲线使用同一映射。组合有限，任意 20 个线路 ID 不保证都唯一；多线路辨识仍结合名称、选择和高亮，不仅靠颜色。
- 手机缩减辅助行与区块间距。加载、空记录、首次失败共用 HistoryState，统一图标、居中方式与画布高度；刷新失败保留旧曲线和明确错误提示。
- 不新增用户设置，不改变后端接口或已有偏好语义。

## 验收结果

| 项目 | 结果 |
| --- | --- |
| lint、业务测试、构建和打包 | 通过，425 处字面量翻译调用检查通过 |
| Chrome 全量 | 122/122，包括新增 12 项比例与提示用例 |
| Edge 重点回归 | 58/58，详情比例、布局、阅读、稳定性与导航 |
| 布局矩阵 | 既有 320/390/430/720/899/900/1024/1440/1920px 双语、深浅色、四样式；新增 1199/1200px 边界 |
| 首屏资料 | 1440×900、标准名称及两条备注下，硬件资料第一行完整进入首屏 |
| 资料卡片 | 宽屏列宽递减、费用自然高度；中等屏幕费用整行；无横向溢出，既有长 CPU/IPv6/计费与复制回归通过 |
| 提示框 | 320/390px 两类图表左中右取点、关闭、重新打开、范围切换；20 条列表内部滚动，按钮至少 44px |
| 精度与裁切 | 0ms 保留、12.34567ms 显示为 12.3ms；标题与按钮同排、滚动后按钮位置不变，提示和按钮均在绘图区内 |
| 线路样式 | ID 1–20 的颜色/线型组合、图例一致性、换范围和全部隐藏/恢复后的稳定性通过；既有 1/3/6/20 条交互回归通过 |
| 异常状态 | 390/1440px 下正常、加载、空记录、首次失败主体高度差≤2px；既有重试、失败保留曲线和迟到响应测试通过 |
| 手机与兼容 | 390×844 首屏工具栏、长内容展开、搜索多选、复制、返回位置、偏好保存、导入导出、首页地图与卡片通过 |

上述为分层覆盖，不是所有尺寸、语言、数据状态的完整笛卡尔积。交互使用本地演示 API 与故障模拟；复制时效使用可控时钟。

## 视觉证据

- [桌面浅色详情](screenshots/detail-light.png)
- [桌面深色详情](screenshots/v0.0.18/desktop-detail-dark.png)
- [手机详情](screenshots/v0.0.18/mobile-detail-light.png)
- [手机深色详情](screenshots/v0.0.18/mobile-detail-dark.png)
- [手机图表提示](screenshots/v0.0.18/mobile-tooltip.png)
- [多线路选择](screenshots/v0.0.18/routes-search.png)
- [桌面前后对照](screenshots/v0.0.18/compare-desktop-detail.png)
- [手机前后对照](screenshots/v0.0.18/compare-mobile-detail.png)

scripts/capture-detail-proportion.mjs 使用固定演示数据生成对照；交互截图由 scripts/capture-detail-proportion-interactions.mjs 生成。README 的 12 张图片均来自 v0.0.18 构建；首页未变的图片重新截图后内容可能相同。历史验收图片保留在旧版本记录中，不出现在 README。

## 发布与边界

版本 0.0.18；短名 hex；在线更新地址 https://github.com/8bitNull/Monitor-Hex。附件 theme.tar.gz 与 theme.tar.gz.sha256。

实体手机、iOS Safari、系统软键盘、动态地址栏和真实服务器部署未验证。桌面浏览器的手机视口测试不替代实体设备验收；此次发布不代表已部署到用户线上站点。
