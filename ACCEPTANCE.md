# Monitor HEX 0.2.3 检查说明

2026-09-23：v0.2.3 完成紧凑可展开地图、离线卡片与详情状态摘要、线路探测采样新旧提示、手机表格空备注列处理，以及表格设置和主题配置备份体验。生产构建、lint、业务测试及完整 Chrome 回归 245 项通过。安装包、版本元数据、封面和 SHA-256 已核验。以上为本地演示数据验收，不代表已替换生产站点。

2026-09-23：v0.2.2 增加电脑／手机独立的20项表格指标勾选，拆分延迟、丢包率和探测线路，保存并迁移列设置；统一下拉菜单，调整手机网速提示及高负载记录固定关闭入口。构建、lint、业务测试和571项翻译检查通过。完整Chrome回归235项中234项首轮通过，1项仍断言旧流量列位置；更新为拆分网络列后的顺序后，该文件2项复测通过，未为通过测试修改运行时代码。安装包0.2.2元数据、封面与SHA-256核验一致。以上为本地生产构建及演示数据验收，不代表已替换生产站点。

2026-09-23：v0.2.1 完成地图构建期投影与路径简化、提前加载、按帧交互更新、加载状态与失败隔离。生产构建、lint、业务测试、565项翻译检查通过；完整Chrome回归220项通过，追加全屏切换后地图专项9项通过（合计221个不同用例）。安装包版本、地图模块、封面与SHA-256核验通过。性能原始数据、慢网冷／热缓存条件和部署边界见 MAP-PERFORMANCE.md。本次为本地生产构建与演示数据验收，未替换用户生产站点或修改线上响应头。

2026-09-23：v0.2.0 使用全新1600×900主题封面，详情页延迟曲线改为独立的低饱和度配色，深浅色分别适配，图例、采样范围与缩略图颜色同步。构建、lint、业务单测、557项翻译检查及34项详情／网络浏览器回归通过；安装包版本、封面内容和SHA-256核对一致。封面使用真实界面与演示数据。

2026-09-23：v0.1.29 重构README为当前安装与使用说明，发布记录集中到GitHub Releases，替换首页、表格、详情及手机截图。表格移除列设置入口，备注使用彩色标签，表头与内容居中，网速箭头靠近数值，排序下拉框收紧并保留键盘焦点；首页网速提示移至标题右侧，卡片上下行内容居中收拢。构建、lint、业务单测、557项翻译检查和完整212项Chrome浏览器回归全部通过；安装包元数据与SHA-256核对一致。截图使用演示数据及浏览器手机视口模拟。

2026-09-23：v0.1.28 首页表格每页20个节点，备注独立为最后一列，移除独立排序及列预设入口、名称资料图标，恢复电脑端卡片与表格共用地图。构建、lint、业务单测、557项翻译检查和18项表格／地图浏览器回归全部通过，覆盖中英文、明暗主题、320～1440px、分页和详情返回。安装包版本与SHA-256校验一致。截图为演示数据与Chrome视口模拟，手机备注列可横向滑动查看。

2026-09-23：v0.1.27 首页TCP/UDP紧凑居中、8px组内间距与14px短分隔线。构建、lint及3项相关浏览器回归通过（覆盖中英文、320至1440px、长数值与备注）；安装包版本和SHA-256校验一致。截图采用本地演示数据与Chrome移动视口模拟。

2026-09-23：v0.1.26 完成首页单行备注、完整备注弹窗及三栏居中。18项相关浏览器回归中16项首轮通过；更新省略文本的溢出断言及等待延迟加载后，两个受影响文件6项复测全部通过。构建、lint、业务单测、554项翻译检查通过，安装包元数据与SHA-256一致。桌面/手机截图采用本地演示数据和Chrome视口模拟。

2026-09-23：v0.1.25 发布验收通过。首页三栏与备注排版、详情线路入口精简均包含在安装包内。完整Chrome浏览器回归210项全部通过；构建、lint、业务单测及554项翻译覆盖检查通过。安装包元数据和SHA-256校验一致。截图使用演示数据，手机使用浏览器视口模拟。

2026-09-23：v0.1.24 完成14项桌面与手机体验优化。Chrome浏览器回归206项全部通过，构建、lint、业务单测及564项翻译覆盖检查通过。详细范围与移动视口验证限制见 [体验优化记录](UX-IMPLEMENTATION.md)。

当前源码的检查入口如下。测试使用本地演示数据，不代表已完成真实服务器部署验证。

## 最近验证

2026-09-23：v0.1.23 包含首页三层账期、实线延迟曲线、说明精简及前台 30 秒自动同步。生产构建、lint、业务单元测试、532 处字面量翻译检查通过。Chrome 全量 198 项中 197 项首轮通过；1 项仍匹配旧文案“已过期”，按确认方案更新为“已到期”并补充 expired 状态断言后，最终构建相关 4 项（含该失败项、账期布局、自动同步、后台暂停）全部通过。初次功能开发的英文长标题对齐已通过共享网格行修正。安装包目录、0.1.23 元数据及 SHA-256 校验通过。截图为本地生产页面和演示数据，不代表线上安装已完成。

2026-09-23：v0.1.22 桌面排版修订按用户确认效果图完成（含后续顶部工具栏截图要求，最终再次全量 195/195 通过）：左线路／右统计同排、主线与阴影图例、缩略图上方的范围及双击复位提示、底部丢包线路与统计范围说明。主图和缩略图采用独立坐标轴，恢复主图时间刻度。最终构建 Chrome 全量 195/195 通过，含新增 900/1024/1440px 中英文桌面几何及刻度检查；生产构建、lint、业务测试、526 处字面量翻译检查和安装包校验通过。新增测试初稿误用了旧版 Recharts DOM 选择器，改为当前坐标轴标签选择器并等待渲染后通过；未为此修改运行时代码。README 截图由最终构建和演示数据重新生成。同版本重新发布，需重新下载安装包。

2026-09-23：v0.1.22 完成首页延迟指标宽度与垂直对齐、详情页采样摘要、真实缩略图、缩放恢复及手机双行工具栏。生产构建、lint、业务测试和 519 处字面量翻译检查通过。Chrome 最终构建全量 192/192 通过（4 workers），覆盖中英文、明暗主题、响应式、触屏提示、线路选择及缩放统计。首轮 191/192，1 项在页面加载时未找到卡片；期间重新构建可能影响了静态资源读取。固定最终构建后完整重跑通过，未修改测试断言或运行时代码来消除该失败。安装包根目录、版本元数据及 SHA-256 已校验；实际截图见 screenshots/v0.1.22。

2026-09-23：v0.1.21 完成首页紧凑网络区和详情页横向概览升级；生产构建、静态检查、业务单元测试及 507 处字面量翻译检查通过。响应式覆盖 320/390/768/1024/1440px、中英文及明暗主题；验证真实采样、离线清空、零值、备注展开、价格右对齐和全宽图表。Chrome 全量 191 项首次 182 项通过，9 项因旧布局断言失败（旧账期分组位置、卡片资料归属、手机首屏图表位置）；同步断言到确认的新方案后，失败项定向复测 9/9 通过。本次没有因这些断言修改运行时代码。

2026-09-22：v0.1.20 静态检查、业务单元测试、507 处字面量翻译检查与生产构建通过。Chrome 全量 184/184（8 workers）。主题设置卡片信息布局、选择框焦点边框和 PC 地图展示行为已完成回归验证。

v0.1.5 首次 8 workers 全量运行有两项既有详情测试失败：提示框位置读取时元素消失、页面导航超时。未修改相关实现，详情文件连续两轮 24/24 通过，之后 4 workers 全量 178/178 通过。此处保留历史记录，不以重试隐藏结果。

表格截图覆盖 320/390/720/721/900/1440px、中英文和明暗主题。200% 使用 CSS 缩放检查布局及键盘操作，不等同于实体手机或所有浏览器原生缩放验证。桌面长表固定表头和名称列，普通行目标 68px，长内容及异常状态允许增高。

最终同设备三次中位数（v0.1.4 → v0.1.5），以浏览器内首次延迟读数出现后的动画帧记录首屏时间，排除测试驱动等待延迟：20 节点 181→208ms，100 节点 238→278ms，500 节点 423→479ms；主线程任务分别 203→208、291→293、540→554ms。保留测试驱动耗时用于追溯，不混用两种口径。

已知开销：100 节点首屏增加约 40ms（16.8%），略高于约 15% 的调查参考线，而主线程增加约 2ms。早期 500 节点额外开销通过行缓存、固定列布局和减少重复 DOM 得到降低；最终保留更清晰的双行读数和完整资料入口，不宣称无性能退化。测量包含浏览器布局、网络及帧调度，不把小样本波动解释成确定收益。

普通桌面浏览的探测请求数从 12 降为 10，主要因行高及可见区域变化；主动质量排序分别完成 20/100/500 个节点的一次读取，沿用最多两个并发，不新增高频轮询。原始数据在 tests/artifacts/v015-comparison/readiness.json 与 quality-loading.json；后者还保留此前测试驱动计时，不能与新的浏览器内计时直接比较。

本轮仅完成主题的本地生产构建与演示数据验收，线上安装需单独记录。

## v0.1.4 历史验证

2026-09-22：v0.1.4 静态检查、业务测试、481 处字面量翻译检查与生产构建通过。Chrome 全量 169/169；Edge 核心交互、地图全屏和响应式检查 17/17。最终图例间距调整后，Chrome 定向回归 16/16。修改后截图已检查。以上为本地生产构建与演示数据验收，不代表已替换生产站点主题。

同数据三次取中位数（v0.1.3 → v0.1.4）：20/100/500 节点首屏就绪 313→311、575→511、1693→1414ms；主线程任务 275→248、564→499、1673→1388ms。各规模两个版本首屏均请求 3 个可见节点的延迟数据。这是开发机比较，不作为生产性能保证。

标准桌面卡片 661.6→548.7px；390px 手机总览 261.6→175px。手机概览保留常用费用/流量信息，卡片比旧版全部辅助资料折叠时更高，但标准样例第一张卡片底部仍提前约 40px。

对比脚本：`scripts/verify-v014.mjs`。默认使用本地 4176 端口的 v0.1.3 与 4175 端口的当前构建，支持 THEME_BASELINE_URL / THEME_CAPTURE_URL；只处理演示数据。对比截图和 JSON 输出在 tests/artifacts/v014-comparison，安装包不包含这些文件。

## 自动检查

```sh
npm ci
npm run lint
npm test
npm run package
npm run test:e2e
```

测试会启动独立预览服务；如果默认 4173 端口已被占用，先设置 `$env:THEME_DEMO_PORT = '4287'`。

需要 Node.js 24，以及已安装的 Chrome。PowerShell 中运行 `$env:TEST_BROWSER = 'msedge'` 后可用同一命令验证 Edge。

## 本轮新增验证

- `table-upgrade.spec.ts`：八列桌面布局、上传/下载/丢包独立排序、旧列迁移、隐藏方向不复活、手机预设、完整名称、固定表头与返回位置、共享阈值、未知丢包、可见行懒加载、主动排序完整读取、低速/离线历史以及 200% CSS 缩放键盘操作。
- 320/390/720/721/900/1440px × 中英文 × 明暗主题表格截图；桌面默认八列无横向滚动，手机仅表格内部滚动。
- `browse.test.ts`：版本 1/2/3/4 列迁移、设备独立预设、空选择、非法字段、丢包/延迟排序、超时、旧记录及离线排末尾。
- 既有 `table.spec.ts` 保留流量计费、显隐持久化和跨天到期测试；v014 的新访客桌面列数更新为八列，手机独立性断言保留。
- `scripts/verify-v015.mjs`：本地旧/新构建同数据对比。默认全流程；CAPTURE_ONLY 仅截图，PERFORMANCE_ONLY 仅测量，SKIP_SORT 跳过完整排序读取，TABLE_COUNTS 可指定节点规模。首屏指标三次取中位数，完整质量排序请求检查每规模每版本一次。

## 浏览器检查范围

181 项用例按功能组织：

- `v023-polish.spec.ts`：桌面／手机首页搜索、返回顶部、手机卡片辅助信息折叠、详情资料分组、CPU/IP 对齐、费用状态和移动端历史范围工具栏。

- `detail-grid.spec.ts`：详情页模块顺序、统一资料外框、内部列分隔线、白色模块背景、资料图标、重复信息移除和 320/390px 无横向溢出。
- `detail-focus.spec.ts`：三项主指标、长标题/备注、长资料复制区域、提示顺序及刷新时间归属。

- `detail-proportion.spec.ts`：布局边界、三列比例与等高资料外框、首屏资料、提示精度与裁切、线路样式稳定性、加载/空/失败画布。

- `detail-balance.spec.ts`：桌面资料布局、紧凑线路菜单、选中语义、长名称、复制时效、提示关闭；20 条线路多选期间顺序、滚动与焦点稳定，重新打开已选优先及提示顺序。

- `detail-resources.spec.ts`：资源图切换、刷新、窄屏、图形风格、空数据、失败重试、离线与采样间断；601/900/1199px 中英文图标按钮可访问名称及切换。
- `detail-routes.spec.ts`：线路继承、比较、详情布局、缺失线路和长文本。
- `navigation.spec.ts`：节点搜索与切换、状态保留、键盘焦点、精确线路入口、告警历史入口、刷新失败与地图复位。
- `appearance.spec.ts`：配色和四种指标布局的深浅色、窄屏及持久化。
- `table.spec.ts`：流量计费、列控制、到期倒计时。
- `load-alerts.spec.ts`：高负载历史、恢复、中断、持久化与键盘交互。
- `defaults-and-freshness.spec.ts`：站点默认值、用户选择优先级、过期数据与连接恢复。
- `map.spec.ts`、`regions.spec.ts`：桌面地图缩放、拖拽、适配、全屏，以及手机地区筛选。新增 320/390/720px 首次访问、筛选持久化、卡片/表格切换及 720/721px 边界验证，以及地区面板的深浅色布局、选中状态、关闭、滚动恢复和焦点回归。

业务单元测试保存在 `src/lib/*.test.ts`，另有文字翻译覆盖检查。截图输出到忽略的 `tests/artifacts/`，测试运行结果输出到 `test-results/`。

- `language.spec.ts`：320/390/1440px 语言切换、即时翻译、偏好持久化、面板保持打开、焦点与滚动恢复、面板边界。

- `refinement.spec.ts`：320/390/430/768/1440px × 中英文 × 深浅色 × 四种样式，共 80 种组合；检查溢出、数值边界、网速基线、首屏内容、样式偏好兼容及异常数据。

- `detail-polish.spec.ts`：六尺寸、中英文、深浅色与四种指标样式的详情布局、触控区域、英文标签、设置分类与滚动关闭，共 96 种组合。

- `card-finish.spec.ts`：长备注、大金额、无价格/无备注、原生线路选择与继承、地图图标移除。

- `customization.spec.ts`：64 种信息组合、手机独立设置与 720/721px 边界、显式偏好继承、导入导出、重置及八种宽度的列数安全。

- `stability.spec.ts`：返回位置偏差不超过 24px、宽度变化重新定位、节点重排与删除回退、网速单位变化高度稳定、刷新保留历史与去重、旧响应隔离、请求超时、八种宽度 × 四种节点数量。

## 打包与边界

- `detail-reading.spec.ts`：九种宽度下的详情层级与工具栏，中英文、深浅色与四种指标样式；资料展开持久化、导入导出与重置；1/3/20 线路；状态绘图区等高；长备注、复制成功/失败及手机图表点选。

- `usability.spec.ts`：预设推导、相关设置隔离、刷新与导入导出、手机独立/跟随、七种视口 × 中英文 × 深浅色 × 两种预设 × 四种样式，另检查键盘操作、滚动保持、目标尺寸、默认配色文字对比度与减少动态效果。

`npm run package` 生成 `theme.tar.gz` 和 SHA-256 校验文件。安装包根目录包含 `theme.json`、`dist/`、预览与许可证；`REVIEW-*.md` 等评审记录不进入安装包。

历史范围依赖后端实际保留的数据。高负载事件是浏览器本地记录，仅覆盖页面监测期间，不等同于服务端持续告警。线上数据与后端兼容性仍需部署后核对。

手机测试采用浏览器视口模拟，未进行实体手机或 iOS Safari 验证。
