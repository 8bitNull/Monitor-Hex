# Monitor Next v1.4.1 延迟刷新修复验收

本次修复基于本地主题及已获取的 hub 源码，尚未连接用户实际站点，因此不将本地问题直接视为生产环境唯一原因。

## 确认的问题

1. 原请求 hours=24&points=40；hub 的 sample_step 将点数限制为至少 60，24h 对应 24 分钟一个桶。桶的时间戳为桶起点、延迟为桶内中位数，因此即使新增探测记录，时间和数值也可能长时间不变。
2. 原刷新间隔从挂载开始计时，而缓存有效期从响应完成后起算。例如首个响应耗时 5 秒，第 60 秒刷新仍落在有效缓存内，下一次真正请求可能到第 120 秒。

## 修复内容

- 使用 1440 点预算取得分钟级桶；保持完整 24h 服务端丢包汇总，色条和小曲线展示最近 40 个桶。
- 自动轮询从本轮结束后 60 秒调度，保留共享缓存、最多两请求并发、可见加载、后台暂停和返回恢复。
- 卡片与表格增加手动刷新，可绕过已完成缓存；进行中的请求仍合并，避免重复并发。
- 显示最近成功读取时间，和探测采样桶时间分别展示；请求采用 no-store 绕过浏览器 HTTP 缓存。

## 验证

- 类型检查、生产构建、代码检查与六组单元检查通过。
- Chrome 完整回归 21/21、Edge 完整回归 21/21，通过且无跳过/不稳定用例。
- 新测试将首次接口响应延迟 5 秒，随后连续两轮自动刷新均更新数值；立即手动刷新更新；卡片切表格共享缓存；隐藏 120 秒不请求，返回后更新。
- 单元测试验证手动刷新绕过已完成缓存、进行中请求去重、no-store、1440 点参数及读取新数据。
- 回归包含 24h 丢包口径、超时/无数据/错误、延迟排序、100 节点并发限制、外观与费用模块。
- 新增读取时间和刷新按钮已通过截图查看。截图为模拟数据，不是实际节点。

## 交付与边界

版本 1.4.1；安装包根目录结构已检查，压缩大小 1,096,614 字节、解压文件合计 2,642,496 字节、最大单文件 779,969 字节，均在 hub 限制内。

SHA-256：66cdd4afbc5a1da8b8116cfb2a81317da14f63463603bf08c5bebde62bc57a4e。

后台主题页上传新版安装包覆盖升级，保留原 v1.4.0 包可回滚，无后端数据库变更。分钟桶不是逐次实时 ping，也不能让后端未上报的数据自动生成。采样时间不推进时，可用读取时间判断前端是否仍在请求，再检查探测任务、agent 和 hub 记录或代理缓存。未验证用户真实 hub/CDN，未进行手机真机测试。

响应点数增加是换取分钟分辨率的成本；仍只有一个 24h 请求和最多两个并发。延迟排序会读取所有节点，普通浏览仅加载可见节点。演示服务器中的模拟指标仍是测试样例，不能用于判断真实探测是否正在运行。
